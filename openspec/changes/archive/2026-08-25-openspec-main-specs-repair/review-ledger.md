# Review Ledger — openspec-main-specs-repair

**Ronda**: 1 · **Protocolo**: judgment-day (dos jueces ciegos en paralelo, modelo opus)
**Objetivo**: el PLAN (proposal / design / spec / tasks). El change NO fue aplicado.
**Skill Resolution**: none — no hay registro de skills en el proyecto; criterios genéricos.
**Veredicto**: `JUDGMENT: ESCALATED ⚠️`

Ambos jueces corrieron 4 sweeps (techo). Juez A: 22 findings. Juez B: 28 findings.
**Sin contradicciones entre jueces.** Toda diferencia fue de método de conteo, no de hecho.

---

## CONFIRMADOS — ambos jueces, hallazgo equivalente

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JD-001 | judgment-day | `tasks.md:31` (4.8) | BLOCKER | open | **`backend-event-consumer` es un falso positivo y la tarea 4.8 ordena corromperlo.** Sus 2 requisitos "borrados" fueron **renombrados vía `MODIFIED` con header cambiado** por `2026-07-01-backend-event-correctness`: `Cadena superseded en ingesta con optimistic locking` → `…y re-consulta ante race`, y `Compactación de cadena a máximo 10 eventos por path` → `Compactación de cadena — retiene los más recientes`. La main spec tiene correctamente las versiones nuevas, tiene `## Purpose` real y **pasa `validate` hoy**. Ejecutar 4.8 inyecta dos duplicados obsoletos en la única spec de la lista que está sana. (JDA-001 / JDB-001) |
| JD-002 | judgment-day | `proposal.md:28`, `design.md:37`, `spec.md:30` | BLOCKER | open | **La verificación central es un non-sequitur y la guarda nace rota.** «No existe un `## RENAMED` ⇒ son borrados y no renombres» es inválido: los renombres se hicieron vía MODIFIED-con-header-cambiado (JD-001). Peor: la invariante de completitud queda **permanentemente en rojo sobre `backend-event-consumer` en su estado correcto**. El criterio de salida «la guarda pasa en verde» es inalcanzable sin cometer la corrupción de JD-001. (JDA-002 / JDB-002, JDB-004) |
| JD-003 | judgment-day | `proposal.md:17-26`, `design.md:8`, `tasks.md:22-32` | BLOCKER | open | **Hay capabilities archivadas sin main spec alguna, y la guarda es estructuralmente incapaz de verlas.** `agent-change-detection-integrity` (2 req) y `agent-command-dispatch` (4 req) no existen en ningún archivo bajo `openspec/specs/`; sumando `agent-baseline-merge` (1) y `agent-bootstrap-verification` (3), son 10 requisitos en 4 capabilities fuera del inventario. La guarda itera `openspec/specs/*/spec.md` comparando cada una contra *sus propios* deltas — **una capability desaparecida entera es invisible por diseño**, y es la peor clase de daño. (JDA-003 / JDB-003) |
| JD-004 | judgment-day | `proposal.md:28`, `design.md:37` | CRITICAL | open | **No existe UN SOLO bloque `## REMOVED Requirements` en todo el archivo** (34 changes, 471 headers escaneados). Las afirmaciones «los REMOVED explícitos se descontaron» y «son pocos y explícitos» son falsas: no se descontó nada porque no hay nada que descontar. La invariante colapsa a `main ⊇ union(ADDED)` — exactamente el método de unión que `design.md:35` **rechaza por escrito como «más simple y está mal»**. D-1 y D-5 se contradicen al aplicarles los datos reales. (JDA-014 / JDB-005) |
| JD-005 | judgment-day | `design.md:9,17`, `tasks.md:36`, `proposal.md:9` | CRITICAL | open | **Números base obsoletos por exactamente un archive — el mío.** Hoy `openspec validate --specs` da `12 passed, 29 failed (41 items)`, no «9 passed, 31 failed de 40». El 41.º es `notification-payload-contract`, que creó el archive de C46 narrado en el propio proposal. Medí antes de archivar y nunca re-medí. Los criterios de salida «pasa las 40» quedarían satisfechos dejando specs sin contar. (JDA-004 / JDB-008) |
| JD-006 | judgment-day | `design.md:31-33`, `spec.md:55` | CRITICAL | open | **La premisa de D-1 «el orden es determinable sin heurística» es falsa por dos vías.** (a) **7 archives comparten el prefijo `2026-06-23`**, y hay 9 fechas más con 2-5 changes cada una; no hay regla de desempate en ningún documento. (b) El prefijo del directorio **no es la fecha de archive**: `2026-06-23-agent-stability-fixes` se commiteó el `2026-06-26`, `2026-06-27-backend-async-io-fixes` el `2026-06-30`, `2026-06-09-domain-models` el `2026-06-08`. **5 de las capabilities con pérdida tienen deltas del mismo día.** La premisa falsa se está congelando en un `SHALL` normativo. (JDA-008 / JDB-006) |
| JD-007 | judgment-day | `design.md:41-43`, `proposal.md:52` | CRITICAL | open | **Las «tres clases» se presentan disjuntas (21+1+7=29) y se solapan en 6.** `agent-core`, `agent-fanotify-detector`, `agent-baseline`, `agent-config-commands`, `agent-bootstrap` y `agent-journal-integrity` están simultáneamente en el grupo 2 y en el grupo 4. La justificación de D-2 —«el contenido no se toca, ahí la verificación por conteo alcanza»— es **falsa justo para esas 6**: `agent-core` contará 2→2 en el commit mecánico y 2.2 reportará «idéntico, OK» sobre un archivo al que le faltan 9 requisitos. Las «21 simples» son 15. (JDA-012 / JDB-009) |
| JD-008 | judgment-day | `tasks.md:17` (3.1) | CRITICAL | open | **El grupo 3 deja 6 de sus 7 archivos todavía fallando.** 3.1 sólo dice «insertar `## Requirements`», pero 6 de los 7 no tienen `## Purpose` y 3 (`frontend-auth`, `frontend-scaffold`, `frontend-shell`) **no tienen ni título H1** — arrancan directo en `### Requirement:`. La invariante 2 de la propia guarda exige título + Purpose + Requirements. El plan no puede alcanzar su propio estado verde. (JDA-006 / JDB-010) |
| JD-009 | judgment-day | `tasks.md:10` (2.1) vs `tasks.md:18` (3.2) | CRITICAL | open | **La tarea 2.1 destruye contenido original y la nota que lo evitaba está en el grupo equivocado.** 5 de las 21 ya tienen H1 (`# Spec: <cap>`) → doble título. Y **2 tienen `## Purpose` redactado a mano** (`backend-async-consumer`, `backend-heartbeat-hmac`, con prosa real sobre el event loop). 2.1 lo pisa con un marcador de reparación — el pecado exacto que este change existe para deshacer. La nota protectora de 3.2 nombra esas dos, pero 3.2 pertenece al grupo 3 y ellas están en el 2. (JDA-007 / JDB-011, JDB-012) |
| JD-010 | judgment-day | `tasks.md:3` (1.1) vs `spec.md:30` | CRITICAL | open | **`MODIFIED` sin `ADDED` previo es comportamiento indefinido, y ocurre 6 veces en el archivo real** (4× `backend-event-consumer`, 1× `backend-health`, 1× `backend-notifications`). Además la tarea 1.1 y el spec dan **dos definiciones distintas** del conjunto esperado. Según cómo se resuelva, el veredicto de la guarda sobre 3 capabilities hoy verdes es arbitrario. (JDA-015 / JDB-013) |
| JD-011 | judgment-day | `tasks.md:11,19` | CRITICAL | open | **El conteo de `### Requirement:` no detecta el daño que dice detectar.** Es conteo de líneas de header. Pasa sin cambios ante: pérdida de bloques `#### Scenario:`, truncamiento de cuerpos, y —lo peor— **restauración de la versión equivocada**: restaurar `agent-decision-engine` desde el ADDED de junio 17 en vez del MODIFIED de junio 23 da 7 en ambos casos, revirtiendo dos requisitos en silencio. Y para las 6 de JD-007 el conteo es trivialmente idéntico sobre un archivo destripado. (JDA-013 / JDB-018) |
| JD-012 | judgment-day | `proposal.md:5`, `design.md:91` (OQ3) | CRITICAL | open | **La causa raíz está mal atribuida y la decisión de no tocar el CLI se apoya en una premisa nunca verificada.** `5355465` —el commit que el propio proposal nombra— **creó el directorio de archive a mano**, junto con cambios de código, sin ejecutar `openspec archive`. Y el archive real más reciente (2026-08-24, CLI **1.4.1**) produjo un `notification-payload-contract/spec.md` **bien formado**. O sea: el CLI actual archiva bien, y la causa real incluye syncs escritos a mano, contra los cuales el plan no agrega ninguna regla de proceso. (JDA-009 / JDB-015) |
| JD-013 | judgment-day | `proposal.md:13`, `spec.md:32` | CRITICAL | open | **El número titular «44 requisitos en 8 capabilities» es incorrecto y ya está escrito en texto normativo.** Los borrados reales son **42 en 7** capabilities (los otros 2 son los renombres de JD-001), más ~10 sin contabilizar (JD-003). El 44 está en `spec.md:32` como medición: archivar este change escribiría una medición falsa en el registro permanente — el modo de falla exacto que el change existe para prevenir. (JDA-011, corroborado por JDB-001) |
| JD-014 | judgment-day | `tasks.md:26,29` (4.3, 4.6) | CRITICAL | open | **Dos capabilities fueron fusionadas y el método no lo modela.** `agent-baseline-merge` (1 req) vive hoy en `agent-baseline`, y `agent-bootstrap-verification` (3 req) en `agent-bootstrap`. Esos requisitos foráneos **son exactamente los «sobrevivientes»** que el plan cuenta («conserva 1 de 9», «conserva 3 de 7»). D-1 recorre sólo `archive/*/specs/<mismo-nombre>/`, así que un ejecutor aplicándolo al pie de la letra **los descarta**. Los números del plan dependen de una fusión que su método no contempla. (JDA-010 / JDB-019) |

## CONFIRMADO por un juez con evidencia de código verificable (elevado)

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JD-015 | judgment-day | `tasks.md:25` (4.2) | CRITICAL | open | **Caso de resurrección CONFIRMADO, no hipotético.** La última versión archivada de `agent-fanotify-detector / Marcado FAN_MARK_FILESYSTEM…` dice *«El detector SHALL inicializar un grupo fanotify con `pyfanotify 0.3.0`»* y *«**MUST NOT** usar `FAN_REPORT_DFID_NAME` ni `FAN_REPORT_FID`»*. El código actual hace lo contrario: `agent/_fanotify.py:132` ejecuta `flags \|= FAN_REPORT_DFID_NAME`, y el commit `f1e8681` reemplazó pyfanotify por un backend ctypes interno. La tarea 4.2 lo restaura verbatim, plantando un `MUST NOT` normativo **falso** en un artefacto de tesis. La tarea 5.5 dice «registrar como hallazgo» pero **no define ningún mecanismo de detección**. (JDB-007) |

## Menores confirmados

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JD-016 | judgment-day | `proposal.md:56` | WARNING (real) | open | Impact dice «29 archivos»; con la tarea 4.8 el plan toca 30. La inventario nunca se cruzó contra la lista de tareas. (JDA-017 / JDB-020) |
| JD-017 | judgment-day | `proposal.md:58` vs `:60` | WARNING (real) | open | «Tests: suite nueva» y dos líneas después «no toca una línea ejecutable». El grupo 1 es enteramente código. La segunda afirmación es la que hace sonar seguro el rollback de `design.md:85`. (JDB-022) |
| JD-018 | judgment-day | `tasks.md:1-6` | WARNING (real) | open | **La guarda no tiene casa ni punto de enforcement.** No se nombra path, suite ni invocación. No hay harness pytest en la raíz (sólo `agent/`, `backend/`, `frontend/`), **no hay CI ni hooks**. Y hay 8-11 changes sin archivar con tareas pendientes, dos de los cuales apuntan a las dos capabilities más destruidas: la recurrencia está agendada, no es hipotética. (JDA-018 / JDB-014, JDB-015) |
| JD-019 | judgment-day | `proposal.md:38` | WARNING (real) | open | «`infra-compose` (9)» — hoy tiene **10** (el archive de C46 agregó uno). Menor, pero es una cifra de referencia en un documento cuyo argumento entero es que las cifras son evidencia. (JDA-019 / JDB-021) |
| JD-020 | judgment-day | `tasks.md:10` | WARNING (real) | open | El H1 prescrito (`# <cap> Specification`) choca con la convención dominante: **14 specs usan `# Spec: <cap>`** contra 7 con la otra. El plan no elige ni reconcilia. (JDA-020) |
| JD-021 | judgment-day | `design.md:77-83`, `tasks.md:37` | WARNING (real) | open | La guarda sólo se re-corre al final, tras 4 tandas de commits. Una regresión del grupo 2 aparece tres commits después. Correrla después de cada grupo. (JDB-027) |
| JD-022 | judgment-day | `tasks.md:22-32` | WARNING (real) | open | El proposal llama al conteo por archivo «la única defensa real», y **el grupo 4 —el único que cambia contenido— no lo tiene**. Sólo «revisar el diff a mano». (JDB-017) |
| JD-023 | judgment-day | `tasks.md:38` (5.3) | WARNING (real) | open | 5.3 prescribe correr un `openspec archive` **real y mutante** como paso de verificación, usando el camino de código bajo sospecha. Sin dry-run, sin rama, sin rollback que cubra una main spec corrompida por el CLI en el mismo commit. (JDB-016) |
| JD-024 | judgment-day | `spec.md:53-65` | SUGGESTION | open | «idéntico al del delta **de origen**» es ambiguo cuando varios deltas tocan el mismo header, y contradice la regla MODIFIED-gana. Un implementador podría afirmar contra el ADDED y congelar la versión revertida. (JDA-021 / JDB-024) |
| JD-025 | judgment-day | `spec.md:7,19` | SUGGESTION | open | La guarda debe anclar la detección a `^## `: el texto del propio spec nuevo contiene los literales `## ADDED Requirements` etc. en prosa. Sin anclaje, la guarda falla sobre sí misma al archivarse. (JDB-026) |
| JD-026 | judgment-day | `tasks.md:32` | SUGGESTION | open | Cita «D-4 de la spec»; el spec no tiene decisiones numeradas. Referencia colgada en la única tarea que gobierna fidelidad de contenido. (JDB-025) |
| JD-027 | judgment-day | `design.md:33` | WARNING (theoretical) | open | El helper debe implementar semántica `RENAMED`, rama **inalcanzable** porque no existe ningún bloque RENAMED. Implementa el mecanismo que nunca verá y omite el que sí ocurre (MODIFIED-como-renombre). (JDB-028) |
| JD-028 | judgment-day | `design.md:33` | WARNING (theoretical) | open | Matcheo por header exacto: hoy es viable (471 headers escaneados, sin NBSP ni whitespace espurio), pero **63 headers tienen em-dash y 11 backticks**. Cualquier `.strip()`, normalización unicode o matcheo vía shell desalinea en silencio. La tarea 1.1 no especifica el mecanismo. (JDB-023 / JDA-013) |

---

## Afirmaciones del plan que AMBOS jueces verificaron como CORRECTAS

- **110 requisitos invisibles** — exacto (74 en las 22 con header de delta + 36 en las 7 sin `## Requirements`).
- **29 specs con daño estructural** — exacto.
- Las 7 del grupo 3 son exactamente el conjunto sin header de delta y sin `## Requirements`.
- `agent-decision-engine` es la única main spec con `## MODIFIED Requirements`, sin título, tocada por exactamente 3 changes, y le faltan 5.
- **Cero `## RENAMED Requirements` en todo el archivo** — literalmente cierto (pero la conclusión que se derivó de eso es inválida, ver JD-002).
- Los conteos de recuperación de las tareas 4.1-4.7 (9, 9, 8, 5, 5, 4, 2) son aritméticamente correctos. Sólo 4.8 está mal.
- `agent-core` pasó de 9 a 2 requisitos en `5355465` (17 inserciones, 135 borrados).
- `agent/baseline.py` implementa HKDF-SHA256 + AES-256-GCM, como se afirmó.
- Ningún `### Requirement:` se esconde dentro de bloques de código, y todo requisito archivado tiene ≥1 `#### Scenario:`.

## Contradicciones entre jueces

Ninguna. La única divergencia fue de contabilidad (A: «42 en 7 + 10 sin contabilizar»; B: «~48 en 9+»), y ambos coinciden en que **44 en 8 es incorrecto** y en que hay capabilities fuera del inventario.
