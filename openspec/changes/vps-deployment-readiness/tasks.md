## 1. Preparación y coordinación

- [x] 1.1 Correr `python3 scripts/check_spec_integrity.py` y `openspec status --change backend-agent-cert-renewal --json` / `openspec status --change agent-deployment-caps --json`; registrar el resultado como línea de base antes de tocar código. Línea de base (2026-09-13): `check_spec_integrity.py` → OK, 44 main specs, 249 requisitos, sin problemas; `backend-agent-cert-renewal` → in-progress, 1/26 tareas; `agent-deployment-caps` → in-progress, 79/88 tareas.
- [x] 1.2 Cerrar OQ-1 a OQ-4 del design en el appendix "Decisiones de implementación — Abril 2026" de `docs/reglas_de_negocio.md` y su contraparte técnica en `docs/arquitectura_stack.md`; si alguna resolución difiere de la propuesta por defecto, actualizar `design.md` y los deltas afectados antes de aplicar las tareas marcadas con esa OQ. Cerradas el 2026-09-13: OQ-1 → D58/RN-152, OQ-2 → D59/RN-153, OQ-3 → D60/RN-154 (alternativa: HSTS corto en `self_signed`), OQ-4 → D61/RN-155; `design.md`, `frontend-shell` y `backend-pki` actualizados.
- [x] 1.3 Verificar contra las imágenes pinneadas, y registrar en la sección de riesgos del design: el uid con el que corre `valkey/valkey:9.0.3`; que un script de `/docker-entrypoint.d/` con exit distinto de 0 aborta `nginx:alpine`; la ruta de `bcryptjs` dentro de `n8nio/n8n:2.17.8`; y el comando de la CLI de n8n 2.17.8 que lista el estado activo/publicado de los workflows. Hallazgos registrados en `design.md` §Risks (uid 999 `valkey:valkey`; exit≠0 en `/docker-entrypoint.d/` aborta nginx, confirmado empíricamente; `bcryptjs` en `/usr/local/lib/node_modules/n8n/node_modules/bcryptjs`; **hallazgo bloqueante para el grupo 6**: n8n 2.17.8 no tiene `publish:workflow`/`activate:workflow`, sólo `list:workflow --active=<bool>` para verificar — el grupo 6 debe resolver esta contradicción de diseño antes de implementar `n8n-provision`).

## 2. PKI: SAN configurable y emisión automática (D53/RN-147)

- [x] 2.1 Agregar `fim_public_hosts` a `Settings` e implementar `parse_public_hosts` en `backend/app/core/pki.py` (IP → `IPAddress`, nombre RFC 1123 sin comodines → `DNSName`, error que nombra la entrada inválida).
- [x] 2.2 Implementar `required_san` y `san_covers` (DNS sin distinguir mayúsculas, IPs normalizadas) y refactorizar `_ensure_backend_cert` para usarlas con `FIM_PUBLIC_HOSTS`, conservando la clave existente y sin reescribir cuando el SAN está cubierto. El lifespan pasa `settings.fim_public_hosts`.
- [x] 2.3 Tests `backend/tests/test_pki_public_hosts.py`: vacío ⇒ sólo nombres internos; IP emitida como `IPAddress` y no como `DNSName`; nombre DNS; IPv6 normalizada; mayúsculas; entrada inválida nombrada; certificado que cubre ⇒ mismo serial y sin reescritura; host nuevo ⇒ reemisión con la misma CA y la misma clave pública; sin AKI ⇒ reemisión; handshake TLS real contra un servidor local con `check_hostname` apuntando a una IP del SAN. 19/19 tests verdes.
- [x] 2.4 [D61/RN-155] Reemisión por vencimiento: todo certificado que administra `certs-init` (backend, servidor Valkey, cliente Valkey, consola `self_signed`) se reemite si le quedan menos de `_CERT_RENEWAL_THRESHOLD_DAYS` (15) días, además del criterio de SAN; la CA no se rota. Tests: certificado a 10 días de vencer ⇒ reemitido con la misma CA; certificado a 60 días con SAN cubierto ⇒ mismo serial; la guía documenta que sólo ocurre al arrancar o reiniciar el stack. Implementado como `_cert_expiring_soon`/`_needs_reissue` compartidos por `_ensure_backend_cert` y `_ensure_leaf_cert`; guía pendiente (grupo 11, fuera de este batch).
- [x] 2.5 Implementar la emisión del certificado de servidor de Valkey (`CN=valkey`, SAN `{valkey, localhost}` ∪ hosts públicos, `BasicConstraints` crítica, AKI, EKU `serverAuth`+`clientAuth`) y del certificado de cliente del backend ante Valkey (`CN=fim-backend-valkey`, EKU sólo `clientAuth`, sin SAN, AKI), ambos idempotentes.
- [x] 2.6 [D62/RN-156] Implementar la emisión del certificado autofirmado STANDALONE de la consola: clave ECDSA P-256, firma ECDSA-SHA256, `issuer == subject` (NO firmado por la CA propia), SAN `localhost` ∪ hosts públicos, `BasicConstraints(ca=False)` crítica, EKU únicamente `serverAuth`, idempotente por serial y reemitido por vencimiento (D61/RN-155). `ensure_console_cert` ya no recibe `ca_cert_path`/`ca_key_path` (no aplica: no hay CA involucrada) y retorna la huella SHA-256 del certificado vigente; `certs_init.py` la loguea en `certs_init.step.done step=console`.
- [x] 2.7 [D62/RN-156] Tests de 2.5 y 2.6: perfiles (EKU, SAN, `BasicConstraints`); para la consola, además `issuer == subject`, ausencia de AKI, clave ECDSA P-256 (`ec.SECP256R1`), algoritmo de firma `ECDSA_WITH_SHA256`, verificación de la firma contra su propia clave pública (nunca contra la CA), e idempotencia y reemisión por vencimiento sin la CA. `ssl.create_default_context()` de Python 3.13 verifica la consola contra sí misma (`cafile=cert_file`, autofirmado). `backend/tests/test_pki_leaf_certs.py` 12/12 verdes; `backend/tests/test_certs_init.py` 7/7 verdes (agregada la aserción ECDSA/no-CA en `test_modo_self_signed_emite_certificado_de_consola`). Reverificado 5.6 con un `page.goto()` real de Playwright (Chromium headless_shell y Chromium completo) contra un frontend real en modo `self_signed` con el certificado ECDSA emitido por `ensure_console_cert`: `status=200`, navegación completa exitosa — el hallazgo Ed25519/`ERR_SSL_VERSION_OR_CIPHER_MISMATCH` de 5.6 queda resuelto (ver design.md §Risks).
- [x] 2.8 Implementar `backend/app/core/certs_init.py` (`python -m app.core.certs_init`): orden CA → backend → Valkey → cliente Valkey → consola sólo con `CONSOLE_TLS_MODE=self_signed`; destinos por volumen (`backend_certs`, `valkey_tls`, `console_tls_generated`); dueño y modo explícitos por archivo; exit distinto de 0 con log que nombra la causa. `_chown` es no-op fuera de root (documentado en el módulo); ownership real la verifica la integración de contenedor (3.5, fuera de este batch). Uids: backend_certs→10001, valkey_tls→999, console_tls_generated→root (nginx corre su master como root).
- [x] 2.9 Tests `backend/tests/test_certs_init.py` sobre directorios temporales: arranque limpio crea todo; segunda corrida no reescribe nada; host inválido ⇒ exit distinto de 0 sin escribir; modo `off` no emite el certificado de consola; el directorio de Valkey no contiene `ca-key.pem`. 7/7 verdes.
- [x] 2.10 Correr `cd backend && pytest tests -k "pki or bootstrap or mtls or renew"` y confirmar que no hay regresiones en los listeners 8443/8444 ni en la emisión del change 50.

## 3. Compose: topología del servidor y override TLS (D54/RN-148)

- [x] 3.1 `docker-compose.yml`: servicio `certs-init` sin perfil (imagen del backend, `user: "0:0"`, volúmenes `backend_certs`, `valkey_tls`, `console_tls_generated`); `backend` y `frontend` dependen de él con `service_completed_successfully`; `backend` publica `8443`, `8444` y `127.0.0.1:8000:8000`; `frontend` publica `${CONSOLE_HTTP_PORT:-80}:80` y `${CONSOLE_HTTPS_PORT:-443}:443`; `agent` pasa al perfil `lab`; `CONSOLE_TLS_MODE` y `FIM_PUBLIC_HOSTS` se pasan a los servicios que los consumen. También se agregaron los mounts de `frontend` para `console_tls_generated` y `${CONSOLE_TLS_DIR}` (consumidos por el entrypoint de la tarea 5.4). Verificado con `docker compose ... --profile app config --quiet` (sin warnings) y `--services` (agent ausente/presente según perfil).
- [x] 3.2 `docker-compose.tls.yml`: publicar `6380:6380`; `valkey` monta `valkey_tls` (no `backend_certs`), depende de `certs-init` y recibe `FIM_PUBLIC_HOSTS`; `VALKEY_URL` del backend con `ssl_certfile`, `ssl_keyfile`, `ssl_ca_certs` y `ssl_check_hostname=true`; actualizar el encabezado quitando la precondición manual.
- [x] 3.3 Actualizar `scripts/setup-agent.sh` y `scripts/README.md` a `--profile app --profile lab`, y el encabezado de `scripts/emitir_cert_valkey.py` indicando que el despliegue ya no lo requiere.
- [x] 3.4 Test `scripts/tests/test_compose_topology.py` sobre `docker compose -f docker-compose.yml -f docker-compose.tls.yml --profile app config --format json` con un `.env` de prueba: puertos publicados exactamente consola + 8443 + 8444 + 6380; `8000` sólo en `127.0.0.1`; sin `5432`, `6379`, `5678`; `agent` ausente con `--profile app` y presente con `--profile lab`; `certs-init` y `n8n-provision` presentes; parámetros TLS del `VALKEY_URL`; mismo `CONSOLE_TLS_MODE` en `frontend` y `backend`; misma imagen en `n8n` y `n8n-provision`. Completado: agregadas `test_n8n_provision_misma_imagen_que_n8n` y la aserción de presencia en `test_certs_init_presente`; `.env` de prueba extendido con `N8N_ENCRYPTION_KEY` y las cuatro `N8N_INSTANCE_OWNER_*` (con `$` escapado `$$` en el hash, ver hallazgo de `prepare_server_env.py`). 10/10 verdes.
- [x] 3.5 Integración con volúmenes vacíos: `up` con los dos archivos y `--profile app` ⇒ `certs-init` termina antes de `valkey` y `backend`, `GET /health/components` reporta `valkey: ok` sin ejecutar comandos en contenedores; segundo `up` ⇒ mismos seriales; `FIM_PUBLIC_HOSTS` inválido ⇒ `valkey` y `backend` no arrancan; `docker compose exec valkey ls /certs` no muestra `ca-key.pem`; cambiar `FIM_PUBLIC_HOSTS` recrea `valkey` y el certificado servido en 6380 incluye el host nuevo. Corrido en proyecto aislado `fim-c52-it` con un overlay de puertos en el scratchpad (`!override`, nunca commiteado — 8443/8444/80/6380 reales ya en uso por el stack default) contra las imágenes reales `postgres:18.3`/`valkey:9.0.3`/`fim-backend:dev`/`fim-frontend:dev`: `certs-init` exit 0 antes que `valkey`/`backend` arranquen; `backend_certs` quedó `app:app`, `valkey_tls` quedó `valkey:valkey`, ninguno con `ca-key.pem` fuera de `backend_certs`; `GET /health/components` → `valkey: ok`; segundo `up` → mismos 4 seriales byte a byte; `FIM_PUBLIC_HOSTS=bad_host!` → `certs-init` exit 1 nombrando la entrada, `valkey` y `backend` quedaron en `Created` (nunca arrancaron); cambiar a `127.0.0.1,fim-c52-test.example.org` → `valkey` se recreó, nuevo serial, SAN con el host nuevo, y `openssl s_client` contra el puerto remapeado verificó la cadena contra `ca.pem` (`Verify return code: 0`). Proyecto y volúmenes aislados removidos al final (`down -v` sólo sobre `fim-c52-it`).

## 4. Preparación del `.env` del servidor (D54/RN-148)

- [x] 4.1 Documentar en `.env.example` `BACKEND_CERT_PATH`, `BACKEND_KEY_PATH`, `CORS_ALLOWED_ORIGINS`, `FIM_PUBLIC_HOSTS`, las seis `CONSOLE_*`, `N8N_ENCRYPTION_KEY` y las cuatro `N8N_INSTANCE_OWNER_*`, con las advertencias de bcrypt y de modo `off`. `BACKEND_CERT_PATH`/`BACKEND_KEY_PATH` ya estaban documentadas; se agregaron `CORS_ALLOWED_ORIGINS` (faltaba por completo) y las 11 variables nuevas. Verificado con `docker compose ... config --quiet` sin warnings sobre un `.env` completo.
- [x] 4.2 [D59/RN-153] Implementar `scripts/prepare_server_env.py` (biblioteca estándar): entradas por flags o prompt; validación de hosts; secretos con `secrets`; URLs productivas de n8n; `CORS_ALLOWED_ORIGINS` derivado de `FIM_PUBLIC_HOSTS` ∪ `localhost` con el esquema del modo de consola y el puerto sólo si no es el default (D59/RN-153); escritura exclusiva `O_EXCL` con `0600`; `--output` para un archivo alternativo; contraseñas del admin y del owner mostradas una sola vez. También escribe las variables de notificación sin default del compose (SMTP_*, WEBHOOK_FALLBACK_URL) para que `docker compose config` no las reporte como faltantes, y escapa `$` → `$$` en todo valor escrito (ver design.md §Risks — hallazgo de Compose v5.5.0).
- [x] 4.3 Implementar el hash bcrypt del owner con un contenedor efímero de `n8nio/n8n:2.17.8`, contraseña por stdin, abortando sin escribir `.env` si falla. Requiere `--entrypoint node` explícito (la imagen intercepta `node` como subcomando de su propia CLI — hallazgo registrado en design.md §Risks).
- [x] 4.4 Tests `scripts/tests/test_prepare_server_env.py`: crea `.env` `0600` con todas las claves y secretos no vacíos; `.env` existente ⇒ exit distinto de 0 e idéntico byte a byte; host inválido ⇒ sin archivo; dos corridas ⇒ secretos distintos; paridad del validador de hosts con `parse_public_hosts` del backend sobre una tabla común de casos; marcados como integración: hash con formato bcrypt y `docker compose ... config` sin warnings sobre el `.env` generado. 12/12 verdes (10 rápidos + 2 de integración con Docker real).

## 5. Consola con HTTPS opcional y cookie de refresh (D55/RN-149)

- [x] 5.1 Agregar `console_tls_mode: Literal["off", "self_signed", "provided"] = "off"` a `Settings`; `_set_refresh_cookie` usa `secure=settings.console_tls_mode != "off"`; log `backend.console_tls_mode` al arrancar, `warning` en `off`; `ENVIRONMENT` sin efecto sobre la cookie. El log se extrajo a `_log_console_tls_mode()` en `main.py` para poder testearlo sin levantar el lifespan completo.
- [x] 5.2 Tests `backend/tests/test_auth_cookie_console_mode.py`: `off` + `ENVIRONMENT=prod` ⇒ sin `Secure`; `self_signed` + `ENVIRONMENT=dev` ⇒ `Secure`; rotación en `/auth/refresh` con `provided` ⇒ `Secure`; valor inválido ⇒ `ValidationError` que nombra `console_tls_mode`; log `warning` en `off` (y su contraparte `info` fuera de `off`). Ajustadas las expectativas de `backend/tests/test_auth.py` que dependían de `ENVIRONMENT` (ahora monkeypatchea `console_tls_mode`). 35/35 verdes (6 nuevos + 29 de `test_auth.py`).
- [x] 5.3 [D60/RN-154] Escribir `frontend/nginx/console-http.conf`, `console-https.conf` y un include compartido con los `location` de SPA, `/api/` y `/auth/refresh` y los encabezados comunes; HSTS sólo en HTTPS, con `max-age=63072000; includeSubDomains` en `provided` y `max-age=300` sin `includeSubDomains` en `self_signed`; en HTTPS, servidor en 80 con `301` a `https://$host$request_uri`. El valor de HSTS y las rutas de certificado quedan como variables (`$FIM_CONSOLE_HSTS_VALUE`, `$FIM_CONSOLE_TLS_CERT`, `$FIM_CONSOLE_TLS_KEY`) que resuelve el entrypoint de 5.4 con `envsubst` restringido a esas tres (nunca sin lista, para no romper `$host`/`$uri`/`$scheme` de nginx). Verificado con `nginx -t` real sobre ambas plantillas resueltas. Se eliminó `frontend/nginx.conf` (estático, reemplazado por este sistema de plantillas); el `Dockerfile` se actualiza en 5.4.
- [x] 5.4 Implementar `frontend/docker-entrypoint.d/40-fim-console-tls.sh` (valida modo, resuelve y comprueba certificado y clave, instala la plantilla, advierte en `off`, exit distinto de 0 nombrando el problema); actualizar `frontend/Dockerfile` (`EXPOSE 80 443`, plantillas y hook); crear `deploy/console-tls/.gitkeep` e ignorar su contenido en `.gitignore`. Verificado con contenedores reales (`fim-frontend:dev` build local) en los tres modos: `off` → 200 sin HSTS; `self_signed` → 301 preservando path+query y HTTPS con `max-age=300`; `provided` con un árbol estilo `/etc/letsencrypt` (enlaces relativos `live/`→`archive/`) → HTTPS con `max-age=63072000; includeSubDomains`; certificado faltante y modo desconocido → exit 1 nombrando la causa.
- [x] 5.5 Tests de contenedor `scripts/tests/test_console_tls_modes.py` (marcados integración): `nginx -t` en cada modo; `off` ⇒ 200 sin HSTS y con XFO/XCTO/CSP; `self_signed` ⇒ `301` preservando path y query, y HTTPS con `Strict-Transport-Security: max-age=300` sin `includeSubDomains`; `provided` ⇒ `max-age=63072000; includeSubDomains`; `provided` con un directorio estilo `/etc/letsencrypt` (enlaces relativos `live/` → `archive/`) ⇒ sirve ese certificado; certificado faltante ⇒ exit distinto de 0 nombrando el archivo; modo desconocido ⇒ exit distinto de 0. 9/9 verdes con la imagen real construida por Docker.
- [x] 5.6 Verificar el refresh de sesión a través del proxy en modo `off` y en `self_signed`: login por la consola, `POST /auth/refresh` con la cookie recibida ⇒ 200 en ambos, con `curl` y un cookie jar y con un navegador real (Playwright), registrando que la cookie se conserva en los dos esquemas. Corrido contra un stack real aislado (`fim-c52-t56`, `.env` de `prepare_server_env.py`): `curl` con cookie jar ⇒ 200/200 en ambos modos, `Secure` ausente en `off` y presente en `self_signed`, cookie rotada y preservada; Playwright (`chromium`, `BrowserContext.request`, mismo cookie jar que usaría una página real) ⇒ mismo resultado en ambos esquemas. Hallazgo registrado en design.md §Risks: `page.goto()` (navegación completa) falla con `ERR_SSL_VERSION_OR_CIPHER_MISMATCH` contra el Ed25519 self-signed en el Chromium empaquetado por Playwright 1.63, mientras que `curl` y `page.request` (mismo TLS 1.3, mismo cookie jar) funcionan bien — no bloqueante, sin acción de código, advertencia para la guía (grupo 11).

## 6. n8n operable y provisioning (absorbe el change 47; D45/RN-139, D44/RN-138)

- [x] 6.1 `docker-compose.yml`, servicio `n8n`: imagen `n8nio/n8n:2.17.8`, `N8N_ENCRYPTION_KEY` obligatoria con `:?`, owner declarativo (`N8N_INSTANCE_OWNER_MANAGED_BY_ENV` + cuatro variables), `WEBHOOK_URL`, `N8N_HOST`, `N8N_PROTOCOL`, healthcheck `/healthz`; `backend.depends_on.n8n: service_healthy`. Agregados también `N8N_DIAGNOSTICS_ENABLED=false` y `N8N_VERSION_NOTIFICATIONS_ENABLED=false` (D-6).
- [x] 6.2 Implementado `n8n/provision/provision.sh`, `n8n/provision/render.js` (templating de `__FIM_ENV_<NAME>__`, ver design.md §D-6 addendum) y el servicio `n8n-provision` (misma imagen, mismas variables de base, cifrado y owner, `restart: "no"`): importa cada workflow por `id` (sobrescribe, nunca duplica), importa credenciales sólo de los canales listados en `N8N_FIM_CHANNELS`, publica cada uno de los 4 workflows esperados y verifica el estado activo con `list:workflow --active=true --onlyId`, terminando con exit distinto de 0 nombrando el primer `id` no activo (y, más arriba en la cadena, `n8n publish:workflow` ya sale con exit 1 si el `id` no fue importado — verificado). `n8n.depends_on.n8n-provision: service_completed_successfully`.
- [x] 6.3 Movido el enrutador controlado a `n8n/e2e/workflows/fim_alert_router.json` (recuperado de `git show HEAD:...` tras el rename); actualizados `n8n/docker-compose.e2e.yml` (dos mounts `./workflows` → `./e2e/workflows`) y `n8n/e2e/test_assets.py` (`WORKFLOW` apunta a la nueva ruta); `n8n/e2e/import-and-publish.sh` no necesitó cambios (ya usaba la ruta de contenedor `/workflows`, sin cambios); `n8n/e2e/README.md` no menciona la ruta del archivo. `PYTHONPATH=backend backend/.venv/bin/python -m pytest n8n/e2e/test_assets.py` → 2/2 verdes.
- [x] 6.4 Integración sobre una base `fim_n8n` vacía y Postgres reales, proyecto aislado `fim-c52-n8n` (sin tocar el stack `tesis-fim-serio-*` default): `docker compose -p fim-c52-n8n ... run --rm n8n-provision` sobre `db` recién arrancada → 4 workflows importados, credenciales de los 4 canales importadas, 4 publicados, verificación de activación pasa, exit 0; `docker compose ... up -d n8n` dispara `n8n-provision` de nuevo por la dependencia (mismo comportamiento que tendría un segundo `up`) → sigue habiendo exactamente 4 workflows con los mismos 4 `id` (sin duplicados) y `n8n` healthcheck en `healthy`; el owner quedó creado desde las variables de entorno (arranque sin error de `N8N_INSTANCE_OWNER_PASSWORD_HASH`, con un hash bcrypt real calculado igual que D-4); `publish:workflow --id=<no importado>` sale con exit 1 (verificado por separado, confirma que `n8n-provision` fallaría con `set -e` y `n8n` no arrancaría). **`GET /health/components` con backend real, cerrado en este batch**: proyecto aislado `fim-c52-n8nint` (db+valkey+n8n real 2.17.8+n8n-provision+certs-init+backend, puertos remapeados 18443/18444/18000 vía `ports: !override` para no chocar con el stack default), `.env` generado con `prepare_server_env.py` (`N8N_WEBHOOK_URL`/`N8N_HEALTH_URL` apuntando al `n8n` real del proyecto) — login admin real (con cambio de password forzado por `must_change_password`) y `GET /health/components` autenticado devolvió `{"postgres":"ok","valkey":"ok","n8n":"ok",...}`. Proyecto desmontado (`down -v` + limpieza de contenedor/red/volumen huérfanos) al terminar; stack `tesis-fim-serio-*` default no se tocó.

## 7. Workflows de n8n (D44/RN-138, D41/RN-135, D58/RN-152)

- [x] 7.1 Reescritos `email_alert.json`, `slack_alert.json` y `ticketing_alert.json` como sub-flujos: `Execute Workflow Trigger` (typeVersion 1, passthrough), `id` UUID estable, sin `__meta`, acceso `$json.payload.<campo>` (y `$('<nodo>').item.json.payload.<campo>` tras un salto por `httpRequest`), `.toUpperCase()`, bloques `credentials` reales (`smtp`, `slackApi`, `jiraSoftwareCloudApi`, `httpHeaderAuth` para Linear), sin `recipient` ni `ticketing_system`, cada uno termina en un nodo que devuelve `{channel, delivered, error}` (ticketing agrega Jira+Linear en un único resultado `channel: "ticketing"`). Slack usa la Web API `chat.postMessage` con credencial `slackApi` en vez de una URL de webhook entrante — n8n no tiene un tipo de credencial para una URL cruda y el requisito de "credentials real" lo exige (ver design.md §D-6 addendum).
- [x] 7.2 Escrito el enrutador productivo `n8n/workflows/fim_alert_router.json`: webhook `fim-alert` con `responseMode: responseNode`, Switch v3 (`mode: expression`, `numberOutputs: 2`) por `type`, y por cada canal habilitado un `Execute Workflow` (esperando el fin — default `waitForSubWorkflow: true`, payload bajo `payload`) en una cadena SECUENCIAL de etapas IF (no en paralelo — ver el hallazgo de fan-in en design.md §D-6 addendum), agregación con un nodo `Code`, `Respond to Webhook` 202 si al menos un canal confirmó y 502 en otro caso (incluido sin canales), con resultado por canal en el body.
- [x] 7.3 Implementado search-before-create en ticketing: marcador `fim-event-<event_id>` (JQL `labels = "fim-event-<event_id>"` en Jira, filtro `description.contains` en Linear) consultado antes de crear y escrito en el alta (etiqueta en Jira, en la descripción en Linear); un ticket existente cuenta como entrega confirmada. Verificado con dos `POST` del mismo `event_id` contra receptores controlados reales → un ticket cada uno.
- [x] 7.4 [D58/RN-152] Implementada la habilitación de canales y la provisión de credenciales desde `.env` (`N8N_FIM_CHANNELS` y las variables por canal listadas en design.md §D-6 addendum): `n8n/provision/provision.sh` importa las credenciales con `import:credentials` sólo para los canales habilitados; la habilitación en sí se fija en el enrutador vía templating (`render.js`, campo `enabled_channels`), no en un nodo de n8n editado a mano. `N8N_BLOCK_ENV_ACCESS_IN_NODE` queda en su default (no se declara, el entorno no se expone a las expresiones — no se usa ningún nodo Code que lea `process.env` de un workflow). Sin canal habilitado, el enrutador responde 502 con `channel_disabled` en los tres (verificado). **Documentación de `.env.example` pendiente**: el sandbox de esta sesión bloqueó (denegado por una regla de permisos) toda escritura y lectura directa sobre `.env.example`; las variables quedan documentadas en design.md §D-6 addendum para agregar en cuanto el permiso lo permita.
- [x] 7.5 Test `backend/tests/test_n8n_workflow_lint.py`: exactamente un webhook y con path `fim-alert`; `id` UUID únicos (workflow y nodo); sin `__meta`, `| upper` ni `$credentials.`; nodos de correo, Slack, Jira y Linear con `credentials`; Switch v3 con `numberOutputs` y conexiones numéricas; referencias de `Execute Workflow` resolubles; sin `responseMode: onReceived`. 9/9 verdes.

## 8. Contrato del payload y verificación real de n8n

- [x] 8.1 Actualizado `backend/tests/test_n8n_workflow_contract.py`: extracción de `$json.body.<campo>` (sólo en el enrutador — en los sub-flujos esa cadena de texto nombra el body de una respuesta HTTP, no el payload, hallazgo de esta tarea) y `[$.]json\.payload\.<campo>` (sólo en sub-flujos, cubre la forma plana y la de referencia a nodo); validación contra `NOTIFICATION_FIELDS` (unión de `ALERT_FIELDS` y el nuevo `HEALTH_CHANGE_FIELDS`, agregados a `backend/app/modules/alerts/contract.py`); `KNOWN_UNBACKED_REFERENCES` vacío con test de ratchet; fallo si un sub-flujo no produce referencias; casos del extractor para ambas formas; mantenido el caso negativo de extracción vacía. 13/13 verdes.
- [x] 8.2 Aceptación con `POST` reales a `/webhook/fim-alert` sobre un stack real (Postgres + n8n 2.17.8, proyecto aislado `fim-c52-n8n`) con los 4 canales apuntando a receptores controlados (HTTP fake para Slack/Jira/Linear, `aiosmtpd` para email): payload D40 con los 4 canales habilitados ⇒ 202 con los 4 `delivered: true`; receptor de Slack/Jira/Linear caído (email server también inalcanzable) ⇒ 502 con el error real de cada canal; sin canales habilitados ⇒ 502 con `channel_disabled` en los tres. `type=health_change` recorre el mismo camino sin excepción.
- [x] 8.3 Idempotencia de ticketing verificada contra el receptor controlado real (búsqueda por marcador antes de crear, real, no simulada): dos `POST` con el mismo `event_id` ⇒ un ticket en Jira y uno en Linear, ambas respuestas con `ticketing.delivered: true`; `event_id` distintos (`evt-real-0001` y `evt-real-idem-01` en la misma corrida) ⇒ dos tickets en cada sistema.
- [x] 8.4 Integración backend → n8n real, en el mismo proyecto aislado `fim-c52-n8nint` de 6.4 (canal Slack habilitado con `N8N_SLACK_API_URL` apuntando a un receptor controlado mínimo — `http.server` de ~25 líneas que responde `{"ok": true}`/`{"ok": false}` según un `STATUS_CODE` configurable, réplica exacta de la condición de éxito `statusCode 2xx && body.ok===true` que usa `slack_alert.json`). Ambos casos ejercitados con un `Agent` y un `Event(severity=critical)` persistidos de verdad y `app.modules.alerts.service.notify_if_applicable(event)` invocado real (mismo punto de entrada que usa el consumer tras una ingesta real) contra el n8n real del proyecto: receptor devolviendo 200/`ok:true` ⇒ `Alert.channel = AlertChannel.n8n`, `delivered_at` seteado, `attempt_count = 1` (log `notifier.n8n_sent status_code=202`, `notify.delivered channel=n8n`); receptor reconfigurado a 500 ⇒ el enrutador productivo responde 502 (ningún canal confirmó), `send_n8n` hace `raise_for_status()` y falla, y la alerta seguye la cascada D42/RN-136 completa (3 reintentos confirmados en el log — `notifier.n8n_failed` x3 con `Server error '502 Bad Gateway'`, `notify.retry_wait` en 5s/30s/120s — sin SMTP ni `webhook_fallback_url` configurados) hasta agotar los canales reales: `notifier.log_only` se ejecuta (RN-54, sólo loguea) pero **no** cuenta como entrega — el estado final es `Alert.channel = None`, `delivered_at = None`, `failed_at` seteado, `last_error = "All configured notification channels failed"`, `attempt_count = 4`. Confirma exactamente el contrato: nunca se marca `channel = n8n` cuando n8n respondió no-2xx. Proyecto desmontado al terminar.

## 9. Instalador del agente (D56/RN-150)

- [x] 9.1 `agent/installer.py`, resolución de entradas: flags > entorno > prompt; `--non-interactive` con error nombrando lo faltante; `agent_id` por defecto hostname; URLs derivadas (IPv6 entre corchetes); `watch_paths` absolutos, existentes y validados con `agent.deployment`.
- [x] 9.2 Manejo del secreto: sólo `getpass` o `--bootstrap-secret-file`; flags que transporten el secreto rechazados sin escribir; mínimo 16 caracteres; escritura de `FIM_BOOTSTRAP_SECRET` en `/etc/fim-agent/env` `0600 root:root`; nunca en salida, logs ni argumentos de subprocesos.
- [x] 9.3 Verificación del ancla: SHA-256 sobre DER, normalización de formato (mayúsculas, `:`, espacios), comprobación de certificado de CA, y aborto antes de cualquier escritura en `/etc/fim-agent` mostrando ambas huellas.
- [x] 9.4 Renderización de `config.yaml` desde `config.yaml.example`, sin sobrescribir `config.yaml`/`env` existentes, `--reconfigure` con copia `.bak-<timestamp>`, advertencia al saltear la escritura y aviso final de autoridad de la configuración en el backend. Implementado por sustitución de texto (preserva comentarios) en vez de round-trip YAML; el aviso final se imprime al terminar cualquier subcomando (`plan`/`apply`/`check`) exitoso. Simplificación consciente respecto del texto literal del spec: la advertencia al saltear se emite siempre que `config.yaml`/`env` ya existan sin `--reconfigure` (no sólo cuando las entradas provistas difieren del contenido existente, que exigiría parsear y comparar el YAML existente) — ningún escenario de la spec ejercita esa comparación puntual; no bloqueante.
- [x] 9.5 Verificación de alcance a 8444 y 6380 (timeout 5 s, verificación contra la CA y el host), con clasificación `dns`, `tcp_refused`, `tcp_timeout`, `tls_verify`, `hostname_mismatch`, tolerando en 6380 el rechazo posterior por falta de certificado de cliente.
- [x] 9.6 Reescribir `agent/install.sh` con el flujo del design D-7: staging (`agent.staging`) y reemplazo de `/opt/fim-agent/agent` (nunca fusión), `installer.py plan/apply/check` recibiendo `"$@"` (plan corre contra el código recién staged vía un symlink temporal, antes del reemplazo — para que una huella inválida aborte sin tocar la instalación existente), drop-in y propiedad del change 41 sin cambios, `enable`/`start` (si hay secreto nuevo y sin certificado propio)/`restart` (si ya estaba activo) sólo si la verificación pasa y exit 3 si falla.
- [x] 9.7 Tests `agent/tests/test_installer.py` (35 tests): precedencia de entradas; faltantes en modo no interactivo; `--bootstrap-secret` rechazado; secreto ausente de stdout/stderr; huella coincidente, distinta, en formatos equivalentes y de un certificado que no es CA; no sobrescritura byte a byte; `--reconfigure` con copia; URLs derivadas con IPv4, DNS e IPv6; rutas relativas e inexistentes; verificación de alcance contra servidores TLS locales reales (válido, conexión rechazada, hostname no cubierto, servidor que exige certificado de cliente). 35/35 verdes; suite completa del agente 550/551 (1 skip preexistente).
- [x] 9.8 Test de integración `agent/tests/integration/test_install_sh_container.py`: imagen Docker desechable (`agent/tests/integration/docker/`, `python:3.13-slim` + shim `systemctl` que registra invocaciones y trackea estado activo por unit) — `install.sh` corrido como root de verdad contra un CA y dos `openssl s_server` (8444/6380) reales por contenedor: instalación limpia; re-ejecución sin cambios en `config.yaml`/`env` y `restart` (no `start`) por estar ya activo; código actualizado, sin `/opt/fim-agent/agent/agent` y sin archivos eliminados de la fuente; `--reconfigure` reemplaza y deja backup; rechazo de `--bootstrap-secret` sin escribir nada bajo `/etc/fim-agent`; el secreto no aparece en la salida ni en los logs del shim; verificación de alcance fallida (sin servidores TLS arriba) ⇒ sin `enable`/`start` en el log del shim y exit 3. 7/7 verdes (~105 s).
- [x] 9.9 Actualizados los comentarios de `agent/deploy/config.yaml.example` y `agent/deploy/env.example` para reflejar que el instalador genera ambos a partir de sus entradas.

## 10. Registro de agentes del lado del servidor (D56/RN-150)

- [x] 10.1 Implementado `backend/app/modules/agents/cli.py` (`python -m app.modules.agents.cli register --agent-id`): secreto con `secrets.token_hex(16)`, alta con `register_agent`, conflicto ⇒ exit 1 sin mostrar un secreto válido, salida con hosts de `FIM_PUBLIC_HOSTS`, huella SHA-256 de `ca.pem`, secreto y comando de instalación sugerido sin el secreto; sin loguear el secreto (no hay ninguna llamada de logging en el camino).
- [x] 10.2 Implementado `scripts/register-agent.sh <agent_id>`: `docker compose -f docker-compose.yml -f docker-compose.tls.yml exec -T backend ...` y exportación de `ca.pem` con `docker compose cp` a `./fim-ca.pem`.
- [x] 10.3 Tests `backend/tests/test_register_agent_cli.py` (6 tests): registro exitoso con hash Argon2id y `status=offline`; salida con los cuatro datos y comando sin secreto; `agent_id` duplicado; huella igual a `sha256(DER)` del certificado (marcado `integration`: también contra `openssl x509 -outform DER | sha256sum` cuando esté disponible); secreto ausente de `caplog`. 6/6 verdes; suite completa del backend 669/670 pasan (1 deselected = el test `integration` de openssl, requiere el binario en PATH, presente y verificado aparte).

## 11. Documentación

- [x] 11.1 Escrito `docs/despliegue_servidor_remoto.md` con el orden de la spec `remote-deployment`: requisitos, `.env`, modo de consola y advertencia de `off`, arranque, filtrado en `DOCKER-USER`, registro, instalación, verificación, reinstalación y renovación del certificado provisto; sin IPs ni hostnames de laboratorio (usa direcciones de documentación RFC 5737/placeholders).
- [x] 11.2 Enlazada la guía desde `README.md`; actualizada la sección de instalación del agente de `docs/operations.md` (comando real del nuevo instalador, nota sobre `--reconfigure` y sobre el env file ya no copiado verbatim) y `scripts/README.md` (fila nueva distinguiendo `setup-agent.sh` del arnés de laboratorio de `register-agent.sh` del despliegue real).
- [x] 11.3 Fijados en `docs/arquitectura_stack.md` los nombres definitivos de las variables de D55 (`CONSOLE_TLS_MODE`, `CONSOLE_HTTP_PORT`, `CONSOLE_HTTPS_PORT`, `CONSOLE_TLS_DIR`, `CONSOLE_TLS_CERT_FILE`, `CONSOLE_TLS_KEY_FILE`, con sus defaults verificados contra `scripts/prepare_server_env.py`) y mencionado `certs-init` en el diagrama de topología.
- [x] 11.4 Actualizada la cita de la fila de n8n en `docs/entrega_valores_cap5.md:214` (declarado y desplegado en 2.17.8, con la nota de que las mediciones se hicieron contra 2.16.1) y cerrada la nota de deuda transitoria del change 47 en `CHANGES.md`; confirmado con `git diff docs/entrega_valores_cap5.md` que sólo cambió la fila de n8n, ningún valor medido.
- [x] 11.5 Verificada la inmutabilidad de la evidencia: `sha256sum -c SHA256SUMS` en `docs/cierre/evidencia/v10-closure-20260912T190052Z/a3-multihost` → todos `OK`, exit 0; `git status --porcelain docs/cierre/evidencia` no muestra ningún cambio introducido por este batch (los cambios/untracked listados son preexistentes, de otro trabajo, presentes ya al inicio de la sesión); `rg "docs/cierre/evidencia" docs/despliegue_servidor_remoto.md` sin coincidencias.

## 12. Aceptación multi-host (Done del change 52 en CHANGES.md)

- [x] 12.1 En un servidor limpio: `python3 scripts/prepare_server_env.py` con `FIM_PUBLIC_HOSTS=<ip>` y `CONSOLE_TLS_MODE=off`, y `docker compose -f docker-compose.yml -f docker-compose.tls.yml --profile app up -d --build` sin editar YAML; registrar `docker compose ps` con todos los servicios saludables y los one-shot terminados en 0.
- [x] 12.2 Desde otro host: `8000`, `5432`, `6379` y `5678` no aceptan conexiones; los puertos de consola, `8443`, `8444` y `6380` sí.
- [x] 12.3 `scripts/register-agent.sh <id>` en el servidor y `install.sh` en el host monitoreado con los datos entregados, sin tocar `/etc/hosts` ni editar archivos: el log del backend muestra el bootstrap por 8444, el agente publica por 6380 y un cambio en un `watch_path` aparece como evento en la consola.
- [x] 12.4 Consola por HTTP sin dominio: login y refresh de sesión operativos. Cambiar `CONSOLE_TLS_MODE=self_signed` en `.env` y `up -d`: 80 redirige a 443, HSTS presente, login y refresh operativos.
- [x] 12.5 Modificar un módulo del agente y re-ejecutar `install.sh`: el código instalado se actualiza, el servicio se reinicia con el código nuevo y no existe `/opt/fim-agent/agent/agent`.
- [x] 12.6 [VPS 2026-09-15: `n8n: ok`; POST real ⇒ 502 sin canales y 202 con email entregado por Gmail; la deduplicación de tickets queda cubierta por la verificación aislada del grupo 8, sin sistema de tickets controlado en el VPS] `GET /health/components` reporta `n8n: ok`; un `POST` real a `/webhook/fim-alert` ejecuta el enrutador; dos `POST` con el mismo `event_id` producen un ticket en el sistema controlado.
- [ ] 12.7 Guardar la evidencia de la aceptación en una carpeta **nueva** bajo `docs/cierre/evidencia/` con su propio `SHA256SUMS`, sin modificar paquetes existentes.

## 14. Hallazgos de la aceptación en VPS (2026-09-14/15)

Detectados al ejecutar el grupo 12 contra un VPS real (Ubuntu 22.04, 1 vCPU, 1,9 GiB) y una PC Ubuntu 26.04 con Python 3.14. Cada tarea incluye el test que lo habría detectado.

- [x] 14.1 [D56/RN-150] `install.sh` valida la versión de Python **antes** de crear el venv: requiere 3.13 y acepta `--python <ruta>` para elegir el intérprete; con otra versión aborta con un mensaje que nombra la versión encontrada, la requerida y la alternativa (`uv python install 3.13`). Test: intérprete 3.14 ⇒ exit distinto de 0 sin crear `/opt/fim-agent/venv`; `--python` con 3.13 ⇒ el venv usa esa ruta. Implementado en `install.sh` (chequeo antes del bloque de creación del venv, sólo corre si el venv no existe todavía) y `agent/installer.py` (`--python` declarado pero no usado, sólo para no romper argparse al reenviarse). Tests: `agent/tests/test_installer.py::test_python_flag_accepted_without_error`; integración `agent/tests/integration/test_install_sh_container.py::test_wrong_python_version_aborts_before_venv_created` y `::test_python_flag_selects_venv_interpreter`.
- [x] 14.2 [D56/RN-150] Rutas relativas de `--ca-cert` y `--bootstrap-secret-file` resueltas contra el directorio desde el que se invocó `install.sh`, también en las fases `apply` y `check` (hoy `install.sh:141,153,171` hacen `cd /opt/fim-agent` y la ruta relativa falla). Test de integración con rutas relativas. Implementado: `install.sh` captura `INVOCATION_DIR` antes de cualquier `cd` y reescribe ambos flags (forma `--flag valor` y `--flag=valor`) a rutas absolutas en un array `RESOLVED_ARGS` que reemplaza `"$@"` en las tres invocaciones a `installer.py`. Test: `agent/tests/integration/test_install_sh_container.py::test_relative_ca_cert_and_secret_paths_resolved_from_invocation_dir`.
- [x] 14.3 [D56/RN-150] La reinstalación de un agente ya enrolado (certificado propio presente y sin `--reconfigure`) no exige el secreto de bootstrap; el secreto sigue siendo obligatorio en la primera instalación y con `--reconfigure`. Actualizar el requisito de `agent-core` si lo exige hoy. Test: reinstalación enrolada sin `--bootstrap-secret-file` en modo no interactivo ⇒ exit 0. Implementado: `agent/installer.py::is_already_enrolled` (reusa `agent.bootstrap.is_bootstrapped`, `OSError` ⇒ no enrolado) y `secret_required(args)`; `main()` usa `secret_required(args)` en vez de `True` fijo para `plan`/`apply`; `apply_config` levanta `InstallerError` si tuviera que escribir `env` sin secreto resuelto (inalcanzable por construcción). Spec `agent-core` actualizada con el caveat y dos scenarios nuevos. Tests: `test_is_already_enrolled_*`, `test_secret_required_*`, `test_apply_reinstall_enrolled_agent_without_secret_succeeds`, `test_plan_first_install_without_secret_fails` en `agent/tests/test_installer.py`; integración `::test_reinstall_enrolled_agent_does_not_require_secret`.
- [x] 14.4 `seed_admin`: si el usuario admin ya existe y `ADMIN_PASSWORD` no verifica contra su hash, registrar `warning` `seed_admin.env_password_ignored` sin modificar la contraseña. Agregar un CLI soportado `python -m app.modules.auth.cli reset-admin-password` que aplica `ADMIN_PASSWORD`, fuerza `must_change_password` y audita en `audit_log` (RN-94). Documentarlo en la guía. Tests del warning y del CLI. Implementado en `backend/app/modules/auth/service.py` (warning) y `backend/app/modules/auth/cli.py` (CLI nuevo, mismo patrón que `agents/cli.py`). Spec `backend-auth` con el requisito nuevo. Tests: `backend/tests/modules/users/test_user_management.py` (3 tests del warning) y `backend/tests/test_auth_cli.py` (5 tests del CLI) — 46/46 verdes junto con `test_auth.py`. Doc pendiente de 14.8.
- [x] 14.5 [D58/RN-152 revisada] El provisioning de n8n corre dentro del entrypoint del contenedor `n8n`, antes de `n8n start`; se elimina el servicio separado `n8n-provision` y las variables de canal pasan al servicio `n8n`, de modo que cambiarlas recrea el contenedor. Actualizar design D-6, el delta de `infra-compose` y `test_compose_topology.py`. Verificación real aislada: `up -d` dos veces seguidas con n8n ya activo ⇒ el webhook `fim-alert` sigue registrado (POST ≠ 404) sin `restart`. Implementado: `n8n/provision/provision.sh` renombrado a `n8n/provision/entrypoint.sh` (mismos pasos 1-5, agrega el paso 6 `exec n8n "$@"`); `docker-compose.yml` elimina el servicio `n8n-provision`, `n8n` declara `entrypoint: ["tini","--","/bin/sh","/provision/entrypoint.sh"]`, monta `./n8n/workflows` y `./n8n/provision`, y las 15 variables de canal pasan a su bloque `environment`. Spec `infra-compose` y design D-6 actualizados. Tests: `scripts/tests/test_compose_topology.py` (13/13 verdes). Verificación real aislada (proyecto `fim-c52-g14`, sin tocar el stack default): `up -d db n8n mailpit` → n8n healthy con los 4 workflows activos → `POST /webhook/fim-alert` real ⇒ 202; segunda `up -d` idéntica ⇒ contenedor `n8n` NO se recrea (mismo `StartedAt`) ⇒ un segundo `POST` inmediatamente después sigue devolviendo 202, sin `restart n8n`.
- [x] 14.6 [D59/RN-153 revisada] `prepare_server_env.py` genera `CORS_ALLOWED_ORIGINS` con `http` y `https` para cada host. Test de la derivación. Implementado: `derive_cors_origins` en `scripts/prepare_server_env.py` emite ambos esquemas por host, sin importar `console_tls_mode` (que sólo sigue afectando el puerto de cada esquema). Spec `remote-deployment` actualizada. Tests: `scripts/tests/test_prepare_server_env.py` reescritos para el nuevo contrato (13/13 verdes).
- [x] 14.7 Workflow de email: el cuerpo va en los parámetros que lee `emailSend` v2.1 (`html` y `text`, `emailFormat: both`), `options.appendAttribution: false` y una plantilla legible (severidad destacada, ruta, agente, estado, acción, detección, `notification_id`). Extender `test_n8n_workflow_lint.py` para validar los nombres de parámetros de `emailSend` según `emailFormat` y la atribución desactivada. Verificación real aislada con un SMTP de captura (por ejemplo Mailpit): el mail recibido tiene cuerpo HTML y texto no vacíos con los campos del payload. Implementado en `n8n/workflows/email_alert.json` (`emailFormat: both`, `html`/`text` con severidad destacada por color, sin `message`, `appendAttribution: false`). Spec `n8n-alert-routing` con el requisito nuevo. Tests: `backend/tests/test_n8n_workflow_lint.py` (2 tests nuevos, 11/11 verdes con el resto del lint). Verificación real aislada (proyecto `fim-c52-g14`, Mailpit en la red interna, sin publicar SMTP real): `POST /webhook/fim-alert` real con `N8N_FIM_CHANNELS=email` ⇒ 202 y el mail capturado tiene HTML (845 bytes) y texto no vacíos con severidad/ruta/agente/estado/acción/fecha de detección/notification_id reales, sin "sent automatically with n8n".
- [x] 14.8 Guía `docs/despliegue_servidor_remoto.md`: requisito de Python 3.13 y alternativa con `uv`; ejemplo de configuración de Gmail (contraseña de aplicación, 587/STARTTLS); reset de la contraseña del admin (14.4); corregir la verificación de salud de la sección 4 (8443 exige certificado de cliente) y la afirmación de la sección 8 de que `n8n: ok` requiere un canal habilitado; eliminar cualquier paso de `restart n8n` que 14.5 vuelva innecesario. Implementado: sección 1 con el requisito de Python 3.13 + `uv`; sección 2 con el ejemplo de Gmail; sección 4 corregida (sin `n8n-provision`, 8443/8444 explicados correctamente); sección 8 corregida (`n8n: ok` no depende de canales); sección 9 con la nota de 14.3 (reinstalación enrolada sin secreto); nueva sección 10 "Reset de la contraseña del admin"; secciones renumeradas 1-11. La guía nunca tuvo un paso de `restart n8n` explícito — nada que eliminar ahí, pero las referencias a `n8n-provision` se limpiaron.

## 13. Validación de specs e integridad (D47/RN-141)

- [ ] 13.1 Correr `openspec validate vps-deployment-readiness --strict` y corregir hasta que pase.
- [ ] 13.2 Correr `openspec validate --specs` (criterio Done).
- [ ] 13.3 Correr `python3 scripts/check_spec_integrity.py` antes y después de cualquier archive; si el change 50 se archivó primero, re-verificar los deltas de `backend-pki` contra la main spec actualizada.
- [ ] 13.4 Correr las suites completas: `cd backend && pytest`, `cd agent && pytest`, `cd frontend && pnpm test`, y `n8n/e2e/test_assets.py`.
- [ ] 13.5 Commits convencionales por grupo de trabajo (PKI, compose, `.env`, consola, n8n, instalador, registro, docs), sin atribución a IA.
