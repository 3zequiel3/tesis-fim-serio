> **Orden de despliegue — restricción, no preferencia (D-9 del design).** El grupo 1 (agente) DEBE
> desplegarse **antes** de re-correr las baterías del Capítulo 5 (tarea 12.4 de
> `agent-attribution-and-detection-gap`): el ruido del journal compite por I/O con la latencia de
> detección, que es uno de los indicadores medidos, y cualquier cambio en el agente **posterior** a
> la medición invalida los números. Los grupos 2 a 7 (backend y frontend) no tocan `agent/`, así que
> pueden desplegarse después de la corrida sin invalidarla.

## 1. Agente — bajar el nivel del log (slice 1, antes de la corrida del Capítulo 5)

- [x] 1.1 En `agent/detector.py:567-571`, cambiar `log.warning("detector.out_of_scope_drop", ...)` por `log.debug(...)`. Los argumentos (`path`, `total_drops`) no cambian: el nombre del evento y su contenido son los mismos, sólo baja el nivel.
- [x] 1.2 Comentar el cambio citando **D69/RN-163** y diciendo por qué: la marca de fanotify es de filesystem completo en modo FID (D46/RN-140), así que el descarte es el caso normal y no una anomalía. Nombrar la magnitud medida (2.748.492 descartes con el journal inundado) para que la próxima lectura no reconstruya el razonamiento desde cero ni "arregle" el nivel de vuelta.
- [x] 1.3 Verificar que `self._out_of_scope_drops += 1` sigue **exactamente donde estaba**, antes del log y del `continue`. El contador no cambia de posición ni de semántica: si el incremento se moviera detrás del log, un cambio futuro de nivel podría dejar de contarlo.
- [x] 1.4 Verificar que la property `out_of_scope_drops` (`agent/detector.py:723-725`) **no se toca**, incluido su docstring que cita RN-04.
- [x] 1.5 Verificar que `agent/heartbeat.py:105` **no se toca**: la clave `out_of_scope_drops` ya viaja en el payload junto a `event_drops`. El agente **no cambia su contrato** en esta change. Si el arreglo parece necesitar tocar el heartbeat, detenerse: el design dice que no hace falta, y esa discrepancia es información.
- [x] 1.6 Verificar con `rg -n "out_of_scope_drop" agent/tests/` que **ningún test fija el nivel** de ese log. `agent/tests/test_scope_filter.py` afirma el contador y su presencia en el payload (sección "4.2 out_of_scope_drops en el heartbeat"): esos tests deben seguir verdes **sin tocar el archivo**. Si alguno fallara, el cambio se pasó de alcance.
- [x] 1.7 Correr la suite del agente completa y dejar el resultado en el reporte de apply. Resultado: `629 passed, 1 skipped in 176.91s` (exit code 0); `agent/tests/test_scope_filter.py` con sus 19 casos en verde sin modificaciones.
- [x] 1.8 **Punto de corte del despliegue**: desplegar el agente con este cambio **antes** de re-correr las baterías del Capítulo 5. Dejar constancia en el reporte de apply de que el orden se respetó, con la fecha del despliegue del agente y la de la corrida. Desplegado 2026-09-17 17:44:05 UTC. **Ineficaz**: el journal siguió inundado (82 líneas `"level": "debug"` en los primeros 15 s) porque `agent/logging.py` nunca filtró la salida de `structlog` por nivel — ver el hallazgo D73/RN-167 (1.9–1.12). El redespliegue que hace efectivo el nivel bajado queda cubierto por 1.12.
- [x] 1.9 **Hallazgo post-despliegue de 1.8, D73/RN-167.** En `agent/logging.py` (`configure_logging`), cambiar `wrapper_class=structlog.stdlib.BoundLogger` por `wrapper_class=structlog.make_filtering_bound_logger(log_level_int)`, mismo mecanismo que `backend/app/core/logging.py:245`. Resolver el nivel de forma robusta: un nombre desconocido o mal formado cae a `INFO` sin romper el arranque. Reemplazar `structlog.stdlib.add_log_level` por `structlog.processors.add_log_level` (mismo objeto desde structlog 20.2, pero sin sugerir dependencia del wrapper `stdlib.BoundLogger` que se retira). Comentar citando **D73/RN-167** y por qué: la configuración anterior nunca filtró — el nivel sólo llegaba a `logging.basicConfig`, que no intercepta la salida de `structlog`. Verificado (`rg` sobre `agent/`, excluyendo tests): ningún call site del agente usa `.exception`, `.critical`, `.msg`, `.log`, `getLogger` de stdlib, `.setLevel` ni argumentos posicionales `%s`, así que no hay API stdlib-only incompatible con el wrapper filtrante.
- [x] 1.10 Test nuevo `agent/tests/test_logging_level.py`: a nivel `info` un log `debug` (con `detector.out_of_scope_drop` como caso motivador) no llega a stdout y un `info` sí; a nivel `debug` el `debug` sí llega; `LOG_LEVEL` sobreescribe el nivel pasado por argumento; un nivel desconocido cae a `info`. Resetear `structlog` entre tests (`structlog.reset_defaults()`). Verificado que ningún test existente configura `structlog` globalmente (`rg -n "configure_logging|structlog.configure" agent/tests agent/conftest.py` sin resultados) ni depende de que `capture_logs()` capture líneas `debug` bajo un wrapper filtrante: `capture_logs()` sólo reemplaza la lista de `processors`, no `wrapper_class`, y como ningún test ni fixture llama a `configure_logging()`, el `wrapper_class` global sigue siendo el default no filtrante durante toda la suite existente. Ningún test existente necesitó cambios.
- [x] 1.11 Correr la suite completa del agente y dejar el resultado en el reporte de apply. Resultado: `633 passed, 1 skipped in 165.93s` (exit code 0) — 4 tests más que el baseline de 1.7 (`629 passed, 1 skipped`), exactamente los de 1.10; `agent/tests/test_scope_filter.py` sigue con sus 19 casos en verde sin modificaciones.
- [x] 1.12 **Redespliegue** del agente con D73/RN-167 aplicado, **antes** de re-correr (o de dar por válida) cualquier batería del Capítulo 5 que no se haya corrido ya bajo un agente con este fix. Verificar en el journal del host que, a nivel `info` (default), ninguna línea `"level": "debug"` aparece —en particular ninguna de `detector.out_of_scope_drop`—. Dejar constancia en el reporte de apply de la fecha del redespliegue y de la verificación del journal. Resultado: redesplegado el 2026-09-17 17:57:16 UTC en `pc-ezequiel` (chequeo de alcance a 8444 y 6380 OK); `/opt/fim-agent/agent/logging.py` contiene `make_filtering_bound_logger`; en el journal del proceso nuevo (PID 164925) hay 0 líneas `"level": "debug"` —las 356 líneas `debug` de `detector.out_of_scope_drop` del minuto de la verificación pertenecen todas al proceso anterior (PID 136837), previo al reinicio—.
- [x] 1.13 **Hallazgo post-redespliegue de 1.12, D74/RN-168.** En `agent/detector.py:561-564`, cambiar `log.warning("detector.event_null_path", pid=ev.pid)` por `log.debug(...)`, agregando `total_drops=self._null_path_drops` al log. Agregar `self._null_path_drops: int = 0` junto a `self._out_of_scope_drops` (`__init__`), incrementarlo **antes** del log —mismo orden que `_out_of_scope_drops`— y **sin tocar** la llamada a `self._trace_record("kernel_dropped", ...)` que ya existe en ese bloque. Agregar la property pública `null_path_drops`, con docstring que cite D74/RN-168 y aclare explícitamente que, a diferencia de `out_of_scope_drops`, un valor positivo NO confirma que el objeto perdido estuviera fuera de `watch_paths` —el chequeo de path nulo corre antes del filtro de scope—, así que el contador se documenta como posible brecha de cobertura, no como ruido esperado. Comentar el cambio citando la investigación de causa raíz en `agent/_fanotify.py` (`_open_by_handle` devuelve `None` cuando `open_by_handle_at` falla, típicamente `ESTALE`, contra todos los fds de montaje). En `agent/heartbeat.py:105`, agregar la clave aditiva `null_path_drops` al payload, junto a `out_of_scope_drops`, con el mismo criterio de "0 sin detector" que las claves vecinas. No agregar rate-limit ni resumen periódico (evaluado y descartado en el design, D-11, por el mismo motivo que D-2 descartó esa alternativa para `out_of_scope_drop`).
- [x] 1.14 Tests: en `agent/tests/test_scope_filter.py`, agregar la sección "4.3 null_path_drops en el heartbeat" (nueva, sin tocar la 4.2) con un test que verifica que un heartbeat con detector incluye la clave con el valor acumulado, y otro que verifica `0` sin detector. Revisar `agent/tests/test_detector_multi_event.py::test_fanotify_null_path_dropped` (ya existente): agregar la aserción de que `null_path_drops` vale 1 tras un evento nulo, sin romper la aserción existente sobre la cola. Agregar un test nuevo que verifique explícitamente que `detector.event_null_path` se emite a `debug` y nunca a `warning`. Revisar `agent/tests/test_detection_gap.py::test_overflow_does_not_hit_null_path_discard`: como la aserción original sólo miraba `mock_log.warning`, ampliarla para mirar también `mock_log.debug`, o el cambio de nivel la vuelve un no-op silencioso. Verificar con `rg -n "out_of_scope_drops" agent/tests/` los sitios donde un `MagicMock()` usado como detector en un test de heartbeat no setea explícitamente el atributo nuevo —`json.dumps` no serializa un `MagicMock` no seteado—: `agent/tests/test_scope_filter.py::test_heartbeat_payload_includes_out_of_scope_drops`, `agent/tests/test_stability_fixes.py::test_event_drops_in_heartbeat` (usa `FanotifyDetector.__new__`, requiere `detector._null_path_drops = 0`) y `agent/tests/test_symlink_hardening.py::test_heartbeat_payload_includes_hardlink_suspected`; setear el atributo nuevo en los tres. Hecho: los tres sitios necesitaban el seteo explícito (fallaban con `TypeError: Object of type MagicMock is not JSON serializable` antes del fix); el test original con `_read_fan_events` como `return_value` estático reprocesaba el mismo evento miles de veces en la ventana de 0.05s, así que las aserciones de conteo nuevas usan el mismo patrón de generador (`_batches()`) que `_run_read_loop_once` ya usa en otros archivos, para entregar el evento una sola vez.
- [x] 1.15 Correr la suite completa del agente y dejar el resultado en el reporte de apply. Resultado: `636 passed, 1 skipped in 172.84s` (exit code 0) — 3 tests más que el baseline de 1.11 (`633 passed, 1 skipped`), exactamente los de 1.14; `agent/tests/test_scope_filter.py` sigue con sus 21 casos en verde (19 de la sección 4.2 sin tocar + 2 nuevos de la 4.3).
- [x] 1.16 **Redespliegue** del agente con D74/RN-168 aplicado. Verificar en el journal del host que `detector.event_null_path` ya no aparece a nivel `warning`. Dejar constancia en el reporte de apply de la fecha del redespliegue y de la verificación del journal. Resultado: redesplegado el 2026-09-17 18:45:13 UTC en `pc-ezequiel`; en los 74 s posteriores al reinicio el journal registró 6 líneas en total (5 `info` de arranque y 1 `warning` de `detector.mark_exclusion_skipped_same_fs`), sin una sola de `detector.out_of_scope_drop` ni de `detector.event_null_path`; el código instalado en `/opt/fim-agent/agent/` contiene el contador `null_path_drops`.

## 2. Backend — modelo y migración

- [x] 2.1 En `backend/app/modules/agents/models.py`, agregar a `Agent` el campo `out_of_scope_drops: int | None = Field(default=None)`, junto a `discarded_events`. Comentar citando **D69/RN-163** con el criterio explícito: `None` = "el agente nunca reportó", distinto de `0` = "reportó y no descartó nada"; y que un positivo es **esperado**, a diferencia de `discarded_events`. Nombrar la migración `020`.
- [x] 2.2 En el mismo archivo, agregar `out_of_scope_drops: int | None = None` a `AgentResponse`, con el mismo comentario de nulabilidad que llevan `queue_size` y `discarded_events`.
- [x] 2.3 Crear `backend/db/migrations/020_add_agent_out_of_scope_drops.sql` con el mismo estilo y estructura de comentarios que `019_add_agent_registered_at.sql`: cabecera que explica **por qué** existe la columna (el contador ya viaja en el heartbeat y el backend lo ignoraba), la nota de idempotencia, y la nota de que **las migraciones se aplican a mano (D3)** con la línea `psql $DATABASE_URL -f 020_add_agent_out_of_scope_drops.sql`.
- [x] 2.4 El cuerpo de la migración es **una sola sentencia**: `ALTER TABLE agents ADD COLUMN IF NOT EXISTS out_of_scope_drops INTEGER;`. **No** lleva `UPDATE` de backfill, **no** lleva `SET DEFAULT` y **no** lleva `SET NOT NULL` — a diferencia de `019`. Documentarlo en el comentario: no hay forma de reconstruir el contador previo, y `NULL` es exactamente la respuesta correcta para las filas existentes. Correrla dos veces no debe producir error.

## 3. Backend — ingesta en el consumer de heartbeat

- [x] 3.1 En `backend/app/modules/agents/heartbeat_consumer.py`, junto a la lectura de `discarded_events` (`:91`), agregar `out_of_scope_drops = payload.get("out_of_scope_drops")` con un comentario que cite **D69/RN-163** y diga que es el mismo criterio tolerante que `queue_pressure` y `watch_path_status`.
- [x] 3.2 En el bloque de aplicación (junto a `:147-153`), replicar **exactamente** el patrón de `discarded_events`: si la clave no es `None`, rechazar `bool` con `log.warning("heartbeat_consumer.invalid_out_of_scope_drops", agent_id=agent_id)` —porque `isinstance(True, int)` es `True` en Python—, aceptar `(int, float)` convirtiendo con `int(...)`, y loguear igual para cualquier otro tipo. Una clave ausente **no** toca el valor guardado.
- [x] 3.3 Verificar que ninguna rama de esta ingesta puede interrumpir el procesamiento del resto del heartbeat: un contador malformado nunca debe dejar a un agente sin actualizar `last_heartbeat` ni sin pasar a `online`.
- [x] 3.4 Actualizar el docstring del módulo (`:6-8`), que hoy enumera qué persiste el consumer, para incluir el contador nuevo citando D69/RN-163.
- [x] 3.5 **No** agregar `event_drops` (D-6 del design). Es la otra clave que el consumer ignora, tiene semántica de pérdida y exige la discusión de anomalía que D69 no cierra. Si durante el apply parece natural agregarla, **detenerse**: es una decisión nueva, va al appendix primero.

## 4. Backend — exposición en la API

- [x] 4.1 En `backend/app/modules/agents/service.py`, agregar `out_of_scope_drops=agent.out_of_scope_drops` a `_agent_to_response` (`:102-118`), con el comentario de nulabilidad al estilo de las líneas vecinas. Esto cubre `GET /agents` y `GET /agents/{id}` a la vez, porque las dos rutas pasan por el mismo mapper.
- [x] 4.2 Verificar leyendo el diff que el valor viaja **sin transformarlo**: sin `or 0`, sin `int(...)`, sin default. El nulo debe propagarse solo hasta la respuesta.

## 5. Tests del backend

- [x] 5.1 Test: un heartbeat con `out_of_scope_drops` numérico persiste el valor y los dos endpoints lo exponen.
- [x] 5.2 Test: un heartbeat **sin** la clave, posterior a uno que reportó un valor, deja el valor guardado intacto (no lo resetea a cero).
- [x] 5.3 Test: un heartbeat con un valor no numérico deja el valor intacto, registra el log de inválido y **procesa el resto del heartbeat igual** (el agente queda `online` y `last_heartbeat` se actualiza).
- [x] 5.4 Test: un heartbeat con un valor booleano se rechaza por la misma rama que el no numérico.
- [x] 5.5 Test: un agente que nunca reportó la clave expone `null`, no `0`, en los dos endpoints.
- [x] 5.6 Correr la suite del backend completa y dejar el resultado en el reporte de apply.

## 6. Frontend — tipo y mapper neutro

- [x] 6.1 En `frontend/src/api/agents.ts`, agregar `out_of_scope_drops?: number | null` a la interfaz `Agent`, con el comentario al estilo de los vecinos: cita a **D69/RN-163**, `null`/`undefined` = "nunca reportó" distinto de `0`, y la aclaración de que un positivo es **esperado** (a diferencia de `discarded_events`).
- [x] 6.2 Crear `frontend/src/utils/outOfScopeDrops.ts` con `getOutOfScopeDropsMeta(count: number | null | undefined)`, mapper **puro** de tres estados (`unknown` / `zero` / `positive`) que nunca lanza, siguiendo el contrato de `frontend/src/utils/discardedEvents.ts`.
- [x] 6.3 Las clases de los tres estados usan **paleta neutra** (gris), nunca roja ni ámbar — incluido el estado `positive`. Esta es la diferencia deliberada con `getDiscardedEventsMeta`, que pinta el positivo en rojo porque allí es una detección perdida. Comentar el mapper explicando esa asimetría y citando D69/RN-163, para que una futura "unificación de estilos" no la borre.
- [x] 6.4 El `title` del estado `positive` SHALL explicar que los descartes fuera de scope son esperados y que el contador es la evidencia de que el filtro de scope está funcionando. El del estado `unknown` distingue "nunca reportó" de cero, al estilo del mapper vecino.
- [x] 6.5 **No** reutilizar `getDiscardedEventsMeta` con un parámetro ni generalizar los dos mappers en uno (D-7 del design). Son módulos separados a propósito.

## 7. Frontend — tarjeta del agente y tests

- [x] 7.1 En `frontend/src/components/ui/AgentCard.tsx`, agregar un `OutOfScopeDropsIndicator` que consuma `getOutOfScopeDropsMeta`, con la misma forma que `DiscardedEventsIndicator` (`:54-64`) pero sin su tratamiento de anomalía. Etiqueta en prosa de UI, contenido del contador en `tabular-nums` como sus vecinos.
- [x] 7.2 Ubicarlo en el bloque de presión de cola (`:141-177`), junto a `DiscardedEventsIndicator` (`:147`), de modo que los dos contadores se vean a la vez y su diferencia de tratamiento sea evidente en pantalla.
- [x] 7.3 Comentar la ubicación citando D69/RN-163 y nombrando explícitamente el contraste con `discarded_events`: los dos están al lado y **deben** verse distintos.
- [x] 7.4 Test del mapper (`frontend/src/utils/outOfScopeDrops.test.ts`): los tres estados retornan resultados distintos, ninguno lanza, y ninguno usa la paleta de alarma. El test del color es el que impide que un futuro cambio de estilo convierta el contador en un indicador rojo en silencio.
- [x] 7.5 Test en `AgentCard.test.tsx`: un valor positivo se renderiza con tratamiento neutro; un cero se renderiza como cero; un agente sin el campo muestra el estado desconocido y **no** cero.
- [x] 7.6 Test en `AgentCard.test.tsx`: con descartes locales positivos y descartes fuera de scope positivos a la vez, los dos indicadores se renderizan y sus tratamientos visuales difieren.
- [x] 7.7 Verificar que los tests existentes de `AgentCard` y de `discardedEvents` siguen verdes **sin modificarlos**: esta change no altera la presentación de `discarded_events`.
- [x] 7.8 Correr la suite del frontend completa y dejar el resultado en el reporte de apply.

## 8. Cierre del change

- [x] 8.1 Verificar que la entrada del Change 56 en `CHANGES.md` refleja lo efectivamente implementado. Si el alcance cambió durante el apply, actualizar la entrada: el roadmap es índice, no historia.
- [x] 8.2 Correr `python3 scripts/check_spec_integrity.py` antes y después del archive (D47/RN-141, sección MANDATORIA de `CLAUDE.md`).
- [x] 8.3 Correr `openspec validate agent-scope-drop-observability --strict` y dejar el resultado en el reporte. Resultado: `Change 'agent-scope-drop-observability' is valid`; `scripts/check_spec_integrity.py` → `OK — 54 main specs, 378 requisitos, sin problemas` (8.2, antes del archive).
- [x] 8.4 **No tocar** `docs/cierre/**`, `.env.example`, `docs/trazabilidad_us_tests.md` ni `docs/residuales_declarados.md` en esta change: quedan fuera de alcance.
- [x] 8.5 Documentar en el reporte de apply cualquier suposición que haya aparecido y que D69/RN-163 no cubra —en particular la presentación de `event_drops` (D-6) o un umbral de alerta sobre el contador—. En ese caso **detener el flujo** y cerrar la decisión en el appendix "Decisiones de implementación — Abril 2026" antes de continuar.

> **Suposiciones aparecidas durante el apply (8.5)**: dos hallazgos post-despliegue, ambos cerrados como decisión antes de tocar código — D73/RN-167 (el logger del agente nunca filtraba por nivel, así que D69/RN-163 no tuvo efecto real) y D74/RN-168 (`detector.event_null_path` a `warning`, con path de ubicación desconocida: posible brecha de cobertura, no ruido confirmado). La presentación de `event_drops` (D-6) y la persistencia/presentación de `null_path_drops` siguen fuera de alcance y sin decidir. La migración `020` no se aplicó a ninguna base (D3): queda para el despliegue manual.
