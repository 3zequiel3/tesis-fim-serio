# Matriz de trazabilidad de cierre

> Corte documental del 2026-09-10, actualizado contra HEAD `3bec584`. La línea base proviene de `docs/trazabilidad_us_tests.md`; las filas US-09 y US-23 incorporan evaluaciones nuevas. Una prueba sólo cuenta cuando aserta el criterio correspondiente.

| Historia | Criterio de aceptación | Implementación | Prueba automatizada | Procedimiento manual | Resultado | Evidencia | Estado |
|---|---|---|---|---|---|---|---|
| US-01 | Inicio de sesión: Formulario; tokens JWT 15 min / 7 días con rotación; error genérico; token en memoria; cookie `httpOnly`/`Secure`/`SameSite=Strict`; Argon2id C9; rate limit 5/15 min; scope `password_change_only` | El error genérico quedó cerrado: `test_login_error_es_identico_para_password_mala_y_usuario_inexistente` compara los cuerpos completos de las dos respuestas y verifica que ninguna nombra al usuario probado. Siguen sin test los parámetros Argon2id, los atributos de la cookie, la vida de los tokens y el almacenamiento en memoria. La cookie además **diverge**: `samesite="lax"` y `path="/"` en `auth/router.py` | `test_auth.py` (7), `test_c22_auth.py` (1), `core/test_rate_limit_load.py` (4); detalle [§5.1](../trazabilidad_us_tests.md#51-us-01-inicio-de-sesión-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_auth.py` (7), `test_c22_auth.py` (1), `core/test_rate_limit_load.py` (4); detalle [§5.1](../trazabilidad_us_tests.md#51-us-01-inicio-de-sesión-parcial) | PARCIAL |
| US-02 | Cierre de sesión: Botón; limpieza de token; blacklist del `jti` en Valkey con TTL; redirección; 401 posterior | **El criterio central quedó cerrado.** `test_logout_invalida_access_token` fue reescrito: pide `GET /events` con el mismo token antes (200) y después del logout (401), y comprueba el TTL de la entrada de blacklist. La causa de que nadie lo hubiera cerrado antes es estructural y vale registrarla: `logout` escribe la blacklist con el cliente Valkey **síncrono** y `get_current_user` la lee con el **asíncrono**, y el conftest le daba a cada uno un mock independiente, de modo que la escritura nunca podía leerse. Los fixtures `blacklist_store`/`blacklist_valkey`/`blacklist_client` les dan un único store en memoria. Siguen sin test los dos criterios de cliente (botón, limpieza del token en memoria, redirección) | `test_auth.py` (2); detalle [§5.2](../trazabilidad_us_tests.md#52-us-02-cierre-de-sesión-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_auth.py` (2); detalle [§5.2](../trazabilidad_us_tests.md#52-us-02-cierre-de-sesión-parcial) | PARCIAL |
| US-03 | Renovación de sesión: Refresh anticipado; rotación single-use; blacklist → login; transparencia; multi-key JWT | La rotación single-use está bien cubierta, incluida la ventana de gracia para refresh concurrente, y ahora también que un refresh revocado por logout no reabre la sesión. Sin test: el multi-key signing C8 — la rama existe en `core/security.py` pero `conftest.py:33` fija `JWT_SECRET_PREVIOUS = ""`, con lo que nunca se ejecuta — y todo el lado cliente | `test_auth.py` (6), `test_c22_auth.py` (2); detalle [§5.3](../trazabilidad_us_tests.md#53-us-03-renovación-automática-de-sesión-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_auth.py` (6), `test_c22_auth.py` (2); detalle [§5.3](../trazabilidad_us_tests.md#53-us-03-renovación-automática-de-sesión-parcial) | PARCIAL |
| US-04 | Métricas del dashboard: Conteo por los 7 estados; léxico en minúsculas C1; carga al entrar; indicadores numéricos | Los cuatro criterios asertados sobre el componente real. El test mockea `@/api/client` y no `@/api/dashboard`, así que ejercita la agregación real del cliente (N requests a `/events`, una por estado). **Encontró un defecto real**: el dashboard enumeraba 6 de los 7 estados canónicos — faltaba `superseded` en `EVENT_STATUSES` (`Dashboard.tsx`) y en `statuses` (`api/dashboard.ts`) —, de modo que los eventos `superseded` no aparecían en ningún contador. Corregido en ambas listas | `Dashboard.test.tsx` (4); detalle [§5.4](../trazabilidad_us_tests.md#54-us-04-métricas-generales-del-dashboard-completa) | No requerida para la clasificación automatizada; recomendable validación de aceptación. | Todos los criterios sustantivos tienen aserciones según la revisión manual. | `docs/trazabilidad_us_tests.md`; `Dashboard.test.tsx` (4); detalle [§5.4](../trazabilidad_us_tests.md#54-us-04-métricas-generales-del-dashboard-completa) | COMPLETA |
| US-05 | Estado general del sistema: Pendientes sin resolver; conectividad de agentes; realce de críticos; banner rojo; banner amarillo | Cuatro de los cinco criterios quedaron asertados sobre componentes reales, incluido el realce visual de los `pending critical + high` con su caso negativo. El quinto no cierra por la regla de §2: la historia condiciona el banner amarillo a `retry_count >= 3` y `AlertsBanner` lo muestra ante **cualquier** alerta fallida, sin ese umbral; el test asserta la conducta implementada, no la pedida | `Dashboard.test.tsx` (5), `SystemBanner.test.tsx` (4), `AlertsBanner.test.tsx` (5); detalle [§5.5](../trazabilidad_us_tests.md#55-us-05-estado-general-del-sistema-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `Dashboard.test.tsx` (5), `SystemBanner.test.tsx` (4), `AlertsBanner.test.tsx` (5); detalle [§5.5](../trazabilidad_us_tests.md#55-us-05-estado-general-del-sistema-parcial) | PARCIAL |
| US-06 | Listado de eventos: Tabla paginada 50/página; fila con path, estado, acción, severidad, fecha y proceso causante; orden desc; excluye `superseded` | Change `frontend-severity-triage` (2026-08-21) cerró el cuarto criterio: `EventsTable.tsx` ahora renderiza severidad (banda de borde + texto canónico) y proceso causante como sublínea del path, con caso negativo para el evento sin contexto de proceso. El "tipo de acción" del criterio se satisface por la columna Estado (D35/RN-129: el `status` derivado *es* la acción ejecutada), sin campo nuevo. Los cuatro criterios quedan asertados sobre el componente real | `test_event_listing_contract.py` (4), `test_c31_backend_event_correctness.py` (1), `eventFilters.test.ts` (2), `EventsTable.test.tsx` (5); detalle [§5.6](../trazabilidad_us_tests.md#56-us-06-listado-de-eventos-completa) | No requerida para la clasificación automatizada; recomendable validación de aceptación. | Todos los criterios sustantivos tienen aserciones según la revisión manual. | `docs/trazabilidad_us_tests.md`; `test_event_listing_contract.py` (4), `test_c31_backend_event_correctness.py` (1), `eventFilters.test.ts` (2), `EventsTable.test.tsx` (5); detalle [§5.6](../trazabilidad_us_tests.md#56-us-06-listado-de-eventos-completa) | COMPLETA |
| US-07 | Filtrado por estado: Selector de los 7 estados; multi-selección; `superseded` excluido; toggle; actualización dinámica | La selectividad real del filtro quedó probada: `test_pagination_total_respects_status_filter` verifica que `total` cuenta sólo el estado pedido y que el ítem devuelto es de ese estado, y `test_status_filter_accepts_multiple_values` que el parámetro repetible es una unión. Siguen sin test los tres criterios de interfaz, y el selector **enumera 6 estados**, no 7 | `test_event_listing_contract.py` (3), `eventFilters.test.ts` (4); detalle [§5.7](../trazabilidad_us_tests.md#57-us-07-filtrado-de-eventos-por-estado-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_event_listing_contract.py` (3), `eventFilters.test.ts` (4); detalle [§5.7](../trazabilidad_us_tests.md#57-us-07-filtrado-de-eventos-por-estado-parcial) | PARCIAL |
| US-08 | Detalle de un evento: Vista de detalle; campos; timestamps dobles con clock skew W13; contexto forense del proceso; enlace al padre; posición en la cadena | Dos huecos grandes cerrados. `test_get_event_by_id_returns_full_detail` es el primer test de camino feliz del detalle: 200 y el conjunto de campos, con el contexto forense (`process_pid`/`uid`/`exe`) verificado ya persistido y expuesto, no sólo capturado en el agente. Y la frontera del clock skew quedó fijada a 299 s / 301 s en las **dos** ramas (con `sent_at` y por fallback sobre `detected_at`), incluida una aserción de que `_CLOCK_SKEW_S == 300`. Falta "tipo de acción", que no existe como campo, y la posición en la cadena | `test_event_listing_contract.py` (2), `test_stream_ack_durability_consumer.py` (11), `test_consumer.py` (1), `test_c31_backend_event_correctness.py` (2), `test_event_severity.py` (1), `test_event_symlink_metadata.py` (2), `test_event_ack_status_field.py` (3), `test_detector_context.py` (4); detalle [§5.8](../trazabilidad_us_tests.md#58-us-08-detalle-de-un-evento-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_event_listing_contract.py` (2), `test_stream_ack_durability_consumer.py` (11), `test_consumer.py` (1), `test_c31_backend_event_correctness.py` (2), `test_event_severity.py` (1), `test_event_symlink_metadata.py` (2), `test_event_ack_status_field.py` (3), `test_detector_context.py` (4); detalle [§5.8](../trazabilidad_us_tests.md#58-us-08-detalle-de-un-evento-parcial) | PARCIAL |
| US-09 | Diff de un evento: unified patch textual; rechazo de UTF-8 inválido, controles y binarios; revalidación backend; límite 1 MiB UTF-8; persistencia `hash_expected`/`diff_text`; lista sin diff y detalle autenticado con diff; render verbatim multi-hunk; no loguear diff | Implementada en `8039624` y completada por `f08626a`. El agente genera el patch sólo para texto admisible; backend revalida y persiste; el endpoint de lista no expone contenido y el detalle autenticado sí; `DiffViewer` renderiza hunks como texto. | Verificación dirigida inicial: 41 pruebas backend/agente + 7 frontend, TypeScript y diff check. Verificación del snapshot limpio compuesto por ambos commits: compilación y serialización PASS, 10/10 agente y 10/10 backend. Los grupos pueden superponerse y no se suman como suite total. | Revisión visual reproducible opcional: abrir un evento textual, confirmar encabezados/hunks y que un evento binario no muestre falso diff. No es necesaria para la clasificación automatizada. | Todos los criterios funcionales enumerados tienen implementación y verificación dirigida sobre bytes aislados. | `8039624292a1f84d6136dd5ed2b2e59d9bdb6f9b`; `f08626a758899d7bb701b55025ebd13f29c17240`; `agent/tests/test_detector_diff.py`; `backend/tests/test_event_listing_contract.py`; `backend/tests/test_event_service.py`; `frontend/src/components/ui/DiffViewer.test.ts`; `frontend/src/pages/EventDetail.test.tsx` | COMPLETA |
| US-10 | Cadena de eventos: Acceso a la cadena del path; orden cronológico con marca de `superseded`; navegación; ícono de cadena rota y `parent_event_id` | Sin cambios. El **modelo de datos** de la cadena está entre lo mejor cubierto del repositorio (supersesión optimista, `parent_event_id`, compactación a 10, protección por `audit_log`, carreras). La **funcionalidad de usuario** no existe: no hay endpoint que devuelva la cadena de un path, y `EventTimeline.tsx` documenta que quedó fuera de alcance | `test_event_service.py` (5), `test_event_status_derivation.py` (2), `test_c31_backend_event_correctness.py` (3), `test_c22_fk_chain.py` (3), `modules/events/test_retention.py` (3); detalle [§5.10](../trazabilidad_us_tests.md#510-us-10-cadena-de-eventos-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_event_service.py` (5), `test_event_status_derivation.py` (2), `test_c31_backend_event_correctness.py` (3), `test_c22_fk_chain.py` (3), `modules/events/test_retention.py` (3); detalle [§5.10](../trazabilidad_us_tests.md#510-us-10-cadena-de-eventos-parcial) | PARCIAL |
| US-11 | Aprobación de un evento: Botón; UPDATE optimista C5; `approved` + `resolved_at`/`resolved_by`; baseline con hash actual; comando firmado C7 + C11; agente rechaza firma/versión; re-cifrado W10; `event_ack` C3; warning de archivo ausente; `audit_log`; 409 + toast | El router conserva `ConflictError → 409`, confirmación explícita y respuesta releída. `8d37075` cerró el defecto de contenido: el detector guarda un candidato cifrado ligado al `source_event_id`, el comando lo transporta y el agente sólo promueve baseline cuando identidad, ruta, estado, hash y bytes coinciden; candidato ausente, obsoleto o inconsistente falla cerrado. Siguen pendientes el toast del 409 y el warning visual de archivo ausente | Regresiones en `test_actions.py`, `test_commands.py` y `test_baseline.py`; detalle histórico [§5.11](../trazabilidad_us_tests.md#511-us-11-aprobación-de-un-evento-pending-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | La corrección central de baseline está implementada/probada; permanecen criterios visuales sin evidencia. | commit `8d37075`; tests citados; detalle [§5.11](../trazabilidad_us_tests.md#511-us-11-aprobación-de-un-evento-pending-parcial) | PARCIAL |
| US-12 | Rechazo de un evento: Botón; elección `restore`/`quarantine`; modal ante baseline `absent` C10; no-op con warning; UPDATE optimista; `rejected` + campos; comando firmado C7 + C11; journal pre-acción W2; `audit_log` | `resolved_at`/`resolved_by`, no-op `baseline_absent` y journal pre-acción permanecen cubiertos. `b060e5f` unificó ambos caminos de cuarentena en un store cifrado y durable; `947edb6` agregó retención y migración legacy. Persisten huecos ajenos a ese store: no se aserta el estado final `completed`/`failed` del journal, `ruleset_version` no integra estos comandos por diseño y faltan criterios visuales | Verificación existente más U1 76/76 y U2 50/50 dirigidas sobre snapshots limpios | Pendiente de validación visual y de los criterios remanentes sobre versión congelada. | Cifrado/retención de cuarentena cumplen; la historia completa conserva otros huecos. | commits `b060e5f`, `947edb6`; `test_quarantine.py`, `test_quarantine_maintenance.py`; detalle [§5.12](../trazabilidad_us_tests.md#512-us-12-rechazo-de-un-evento-pending-parcial) | PARCIAL |
| US-13 | Superseded automático: Nuevo evento supersede al `pending`; vínculo `parent_event_id`; no aparece en pendientes; transición validada C2; accesible por el toggle | Era la más cerca de cerrarse y se cerró. `test_list_events_excludes_superseded_by_default` cubre el único criterio que faltaba, y `test_superseded_filter_applies_to_total_not_only_to_the_page` agrega el matiz que hacía falta para que la exclusión sea coherente con la paginación: el `superseded` excluido tampoco cuenta en `total`. El acceso para auditoría queda cubierto por `test_list_events_includes_superseded_when_flag_is_true` | `test_event_listing_contract.py` (3), `test_event_service.py` (7), `test_event_status_derivation.py` (5), `test_c31_backend_event_correctness.py` (2), `test_event_consumer_c11.py` (1), `test_c22_consumer.py` (1); detalle [§5.13](../trazabilidad_us_tests.md#513-us-13-superseded-automático-completa) | No requerida para la clasificación automatizada; recomendable validación de aceptación. | Todos los criterios sustantivos tienen aserciones según la revisión manual. | `docs/trazabilidad_us_tests.md`; `test_event_listing_contract.py` (3), `test_event_service.py` (7), `test_event_status_derivation.py` (5), `test_c31_backend_event_correctness.py` (2), `test_event_consumer_c11.py` (1), `test_c22_consumer.py` (1); detalle [§5.13](../trazabilidad_us_tests.md#513-us-13-superseded-automático-completa) | COMPLETA |
| US-14 | Listado de reglas: Tabla; fila con patrón, severidad y acción; leyenda del default `alert_only`; `ruleset_version` del sistema | Sin cambios. Sólo el listado y su orden canónico por severidad están asertados. El `ruleset_version` global del sistema **no lo expone ningún endpoint**: lo que la UI muestra es `ruleset_version_applied` por agente, que es otra cosa | `test_rules_service.py` (3), `test_rules_router.py` (2), `test_rules.py` (1); detalle [§5.14](../trazabilidad_us_tests.md#514-us-14-listado-de-reglas-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_rules_service.py` (3), `test_rules_router.py` (2), `test_rules.py` (1); detalle [§5.14](../trazabilidad_us_tests.md#514-us-14-listado-de-reglas-parcial) | PARCIAL |
| US-15 | Creación de regla: Formulario; glob y negación `!`; la exclusiva gana; persistencia; patrón no duplicado; `ruleset_version++` C11 + sync; `audit_log` | Sin cambios. La precedencia de reglas exclusivas está probada **sólo en el agente**: `rules/service.py::determine_severity_for_path` hace `fnmatch` sin tratar el prefijo `!`. La validación de patrón duplicado no está implementada | `test_rules_service.py` (10), `test_rules_router.py` (3), `test_ruleset_version_atomic.py` (2), `test_rules.py` (3); detalle [§5.15](../trazabilidad_us_tests.md#515-us-15-creación-de-una-regla-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_rules_service.py` (10), `test_rules_router.py` (3), `test_ruleset_version_atomic.py` (2), `test_rules.py` (3); detalle [§5.15](../trazabilidad_us_tests.md#515-us-15-creación-de-una-regla-parcial) | PARCIAL |
| US-16 | Edición de regla: Formulario precargado; modificar patrón/severidad/acción; persistencia; `ruleset_version++` + sync; `audit_log` | El núcleo de la historia dejó de estar sin asertar: `test_update_rule_persists_the_new_field_values` relee la fila desde la base tras un `expire_all()` —para no leer el objeto en memoria que el servicio ya mutó— y verifica los tres campos, que no se creó una fila nueva y que `updated_at` avanzó; `test_update_rule_partial_payload_keeps_untouched_fields` cubre el payload parcial. Sólo queda sin test el formulario precargado | `test_rules_service.py` (4), `test_rules_router.py` (3); detalle [§5.16](../trazabilidad_us_tests.md#516-us-16-edición-de-una-regla-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_rules_service.py` (4), `test_rules_router.py` (3); detalle [§5.16](../trazabilidad_us_tests.md#516-us-16-edición-de-una-regla-parcial) | PARCIAL |
| US-17 | Eliminación de regla: Opción de borrar; confirmación; eliminación de la fila; `ruleset_version++` + sync; caída al default `alert_only`; `audit_log` | `test_delete_rule_removes_the_row` cierra el criterio central y agrega el control de que sólo se borró la regla pedida. Siguen sin test los dos criterios de interfaz y el criterio 5 conserva cobertura indirecta (el default `alert_only` de `RulesCache.evaluate`), no una prueba del escenario "regla borrada → el path cae al default" | `test_rules_service.py` (3), `test_rules_router.py` (3), `test_rules.py` (1), `test_decision.py` (1); detalle [§5.17](../trazabilidad_us_tests.md#517-us-17-eliminación-de-una-regla-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_rules_service.py` (3), `test_rules_router.py` (3), `test_rules.py` (1), `test_decision.py` (1); detalle [§5.17](../trazabilidad_us_tests.md#517-us-17-eliminación-de-una-regla-parcial) | PARCIAL |
| US-18 | Sync automática de reglas: Publica `rule_sync`; firma C7 + `ruleset_version` C11; el agente verifica firma; descarta versiones menores; reemplaza caché sin reiniciar; persiste en `state.json`; comandos antes que eventos W4; `event_ack` C3 | Sin cambios. Siete de ocho criterios asertados, incluido el orden de reconexión W4 y la persistencia del `ruleset_version` en `state.json`. El único que falla —`event_ack` tras `rule_sync`— no está implementado, y los tests **documentan la decisión contraria** (`test_rule_sync_persists_with_null_ack_status`) | `test_rules_service.py` (8), `test_rule_sync_outbox.py` (4), `test_c32_sse_security_fixes.py` (1), `test_rules.py` (5), `test_stability_fixes.py` (2), `test_reconnect_order.py` (7); detalle [§5.18](../trazabilidad_us_tests.md#518-us-18-sincronización-automática-de-reglas-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_rules_service.py` (8), `test_rule_sync_outbox.py` (4), `test_c32_sse_security_fixes.py` (1), `test_rules.py` (5), `test_stability_fixes.py` (2), `test_reconnect_order.py` (7); detalle [§5.18](../trazabilidad_us_tests.md#518-us-18-sincronización-automática-de-reglas-parcial) | PARCIAL |
| US-19 | Listado de alertas: Lista ordenada por fecha desc; fila con path, severidad, acción, fecha y canal; navegación al evento | Sin cambios. El endpoint, sus filtros y la paginación están bien cubiertos; los tres criterios *de la historia*, no. `AlertResponse` no expone `path` ni tipo de acción, la navegación al evento no está implementada, y el `ORDER BY created_at DESC` no lo asserta ningún test | `test_sse_alerts.py` (10); detalle [§5.19](../trazabilidad_us_tests.md#519-us-19-listado-de-alertas-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_sse_alerts.py` (10); detalle [§5.19](../trazabilidad_us_tests.md#519-us-19-listado-de-alertas-parcial) | PARCIAL |
| US-20 | Alertas en tiempo real: Conexión SSE; alerta ante nuevo evento; notificación visual; reconexión automática | Sin cambios. El backend SSE está bien cubierto: auth por query param, replay con `Last-Event-ID`, keepalive, cierre de sesión de DB y cola acotada a 100. El hueco relevante es la cadena real evento → `notify_if_applicable` → `alerts_broadcaster.publish`, que **ningún test recorre**: de `notify_if_applicable` sólo se prueban los tres casos de *skip* | `test_sse_alerts.py` (12), `test_c32_sse_security_fixes.py` (4); detalle [§5.20](../trazabilidad_us_tests.md#520-us-20-alertas-en-tiempo-real-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_sse_alerts.py` (12), `test_c32_sse_security_fixes.py` (4); detalle [§5.20](../trazabilidad_us_tests.md#520-us-20-alertas-en-tiempo-real-parcial) | PARCIAL |
| US-21 | Estado de agentes: Lista; identificador, estado, última actividad, `ruleset_version`, `queue_size`; realce de no-ok; heartbeat 10 s / 30 s → offline / 5 min → dead + webhook / `shutdown` → draining; banner de `queue_pressure` W3 | La máquina de estados del heartbeat quedó completa en sus transiciones (online → draining → offline@30s → dead@5min → vuelta a online, y ahora también draining → offline). Sin cobertura: el intervalo de 10 s, el webhook n8n al pasar a `dead` (no implementado), `queue_size` en la vista (no se persiste) y el umbral del 80% de W3, que no existe en el código | `test_agent_mgmt.py` (5), `test_heartbeat_consumer.py` (6), `test_agent_watch_path_status.py` (2), `test_domain_models.py` (1), `test_queue.py` (4); detalle [§5.21](../trazabilidad_us_tests.md#521-us-21-estado-de-agentes-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_agent_mgmt.py` (5), `test_heartbeat_consumer.py` (6), `test_agent_watch_path_status.py` (2), `test_domain_models.py` (1), `test_queue.py` (4); detalle [§5.21](../trazabilidad_us_tests.md#521-us-21-estado-de-agentes-parcial) | PARCIAL |
| US-22 | Re-scan de baseline: Botón con selección de paths; diálogo con los `pending` afectados; confirmación explícita; supersesión + comando firmado C7 con `ruleset_version++` C11; el agente verifica firma y versión; regenera baseline cifrado W10; `event_ack` C3; confirmación visual; `audit_log`; deshabilitado si `draining` | Sin cambios. El flujo existe de punta a punta y ese eje está bien cubierto. Tres criterios fallan por implementación: no hay selección de paths, el comando no lleva `ruleset_version++`, y `handle_rescan_baseline` no tiene guard de versión. La supersesión alcanza a **todos** los pending del agente, no a los paths seleccionados | `test_agent_mgmt.py` (4), `test_published_command_ack_tracking.py` (1), `test_stream_ack_durability_outbox.py` (1), `test_agent_config.py` (5), `test_commands.py` (3), `test_baseline.py` (7); detalle [§5.22](../trazabilidad_us_tests.md#522-us-22-re-scan-de-baseline-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_agent_mgmt.py` (4), `test_published_command_ack_tracking.py` (1), `test_stream_ack_durability_outbox.py` (1), `test_agent_config.py` (5), `test_commands.py` (3), `test_baseline.py` (7); detalle [§5.22](../trazabilidad_us_tests.md#522-us-22-re-scan-de-baseline-parcial) | PARCIAL |
| US-23 | Notificación externa: Webhook n8n ante `critical`/`high`; payload con contexto de proceso y timestamps; n8n como enrutador; retry 5/30/120 s; cascada SMTP → webhook → log; fila en DLQ; n8n caído no compromete la operación | `f0a2907` implementa router/receptor controlado y evita falso 200. `e2519eb` persiste en `alerts` el `notification_id`, intentos y próximo retry, recupera al iniciar y continúa la cascada tras cuatro intentos n8n. La tabla real sigue siendo `alerts`, no `failed_notifications`. | Unidad A: 202, receptor caído 502, n8n caído y recuperaciones. Unidad B: 9 pruebas dirigidas PASS; PostgreSQL limpio con cuatro fallos n8n → webhook controlado, fallo total persistido y recuperación tras restart con el mismo ID. La ejecución ampliada fue 55/62; siete fallos de harness mTLS preexistente no se atribuyen a n8n. | Repetir con `n8n/e2e/README.md`. Para completar todos los criterios resta acreditar SMTP real y confirmar en una evaluación final el payload de contexto completo. | Persistencia, reintento, fallo total, recuperación y fallback webhook están verificados. Delays 0 fueron exclusivos del ensayo; producción conserva 5/30/120 s. Garantía al-menos-una-vez con deduplicación por `notification_id`; una instancia backend. | `f0a2907`; `e2519eb`; `3bec584`; `n8n/e2e/evidence/20260909-run.json`; `n8n/e2e/evidence/20260910-durable-fallback.json`; `backend/tests/test_notifications.py`; detalle [§5.23](../trazabilidad_us_tests.md#523-us-23-notificación-externa-ante-eventos-críticos-parcial) | PARCIAL |
| US-24 | Paths monitoreados: Lista de paths por agente; agregar; quitar; persistencia; `update_config` firmado C7 + `ruleset_version++` C11; el agente verifica y recarga en caliente; baseline scan de paths nuevos W10; `event_ack` C3; bootstrap desde YAML → autoridad en PostgreSQL; `audit_log`; deshabilitado si `draining` | Sin cambios. La historia mejor cubierta del lado backend + agente. Se mantiene la advertencia metodológica: `test_reload_watch_paths_marks_new` y `::test_reload_watch_paths_unmarks_removed` **no invocan el método que dicen probar** — reimplementan la aritmética de conjuntos dentro del propio test. La reconfiguración real de las marcas de `fanotify` no está verificada por ningún test | `test_agent_mgmt.py` (5), `test_agent_watch_path_status.py` (3), `test_published_command_ack_tracking.py` (2), `test_agent_config.py` (6), `test_commands.py` (5), `test_stability_fixes.py` (2), `test_audit_fixes.py` (1); detalle [§5.24](../trazabilidad_us_tests.md#524-us-24-gestión-de-paths-monitoreados-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_agent_mgmt.py` (5), `test_agent_watch_path_status.py` (3), `test_published_command_ack_tracking.py` (2), `test_agent_config.py` (6), `test_commands.py` (5), `test_stability_fixes.py` (2), `test_audit_fixes.py` (1); detalle [§5.24](../trazabilidad_us_tests.md#524-us-24-gestión-de-paths-monitoreados-parcial) | PARCIAL |
| US-25 | Bulk approve/reject: Checkbox por fila y "todo en la página"; botones bulk; modal con los primeros 10 paths; elección de acción; `POST /actions/bulk-*`; locking individual C5; `succeeded[]`/`failed[]`; baseline firmado por cada aprobado; `audit_log`; resumen visual; refresco | Change `frontend-severity-triage` (2026-08-21) cerró el defecto que dejaba el bulk reject en 422 el 100% de las veces (`frontend/src/api/actions.ts:64` mandaba `action` al nivel superior; el backend la exige por ítem). `BulkActionBar.test.tsx::rechazar 3 eventos con "quarantine" produce 3 ítems con action:"quarantine" cada uno, sin action al nivel superior del cuerpo` es el test que habría atrapado el defecto original, y cierra con evidencia de frontend la "elección de acción" que antes sólo tenía test de backend. Checkbox por fila, modal con los 10 paths, resumen visual y refresco siguen sin test (UI) | `test_actions_router.py` (10), `test_actions.py` (6), `BulkActionBar.test.tsx` (1); detalle [§5.25](../trazabilidad_us_tests.md#525-us-25-bulk-approvereject-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_actions_router.py` (10), `test_actions.py` (6), `BulkActionBar.test.tsx` (1); detalle [§5.25](../trazabilidad_us_tests.md#525-us-25-bulk-approvereject-parcial) | PARCIAL |
| US-26 | Paginación: 50 por página; navegación numerada; input "ir a página"; `?page=N&page_size=50`; respeta los filtros activos | El riesgo silencioso más grande quedó cubierto con tres tests: `total` respeta el filtro de estado, el `path_prefix` y el rango de fechas — cada uno con `page_size=1` para que un `total` mal calculado no se disimule. Los criterios 2 y 3 siguen sin test porque **no están implementados**: sólo hay "Anterior"/"Siguiente" | `test_event_listing_contract.py` (5), `test_c31_backend_event_correctness.py` (1), `test_event_router.py` (1), `eventFilters.test.ts` (3); detalle [§5.26](../trazabilidad_us_tests.md#526-us-26-paginación-de-eventos-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_event_listing_contract.py` (5), `test_c31_backend_event_correctness.py` (1), `test_event_router.py` (1), `eventFilters.test.ts` (3); detalle [§5.26](../trazabilidad_us_tests.md#526-us-26-paginación-de-eventos-parcial) | PARCIAL |
| US-27 | Cambio obligatorio de password: Seed con el flag; scope `password_change_only`; redirect forzado; formulario con complejidad; validación de la actual; Argon2id C9; flag a `false` + tokens nuevos; bloqueo de otras rutas; re-exigencia; `audit_log` | El gate de scope en backend está bien cubierto. El flujo completo (login inicial → cambio → login con scope pleno) ahora se ejercita como precondición del helper `_full_access_login` de los tests de logout, aunque de forma incidental. Sin test: validación de la contraseña actual, reglas de complejidad (no implementadas), Argon2id, `audit_log` y re-exigencia tras cerrar sesión | `test_auth.py` (5), `test_c22_auth.py` (1), `test_c22_scope_gate.py` (5), `modules/users/test_user_management.py` (3); detalle [§5.27](../trazabilidad_us_tests.md#527-us-27-primer-login-con-cambio-obligatorio-de-password-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_auth.py` (5), `test_c22_auth.py` (1), `test_c22_scope_gate.py` (5), `modules/users/test_user_management.py` (3); detalle [§5.27](../trazabilidad_us_tests.md#527-us-27-primer-login-con-cambio-obligatorio-de-password-parcial) | PARCIAL |
| US-28 | Banner de degradación: Poll cada 10 s; estado de `postgres`, `valkey`, `n8n` y agentes; banner rojo con componente y timestamp; cerrable y reaparece; no bloquea; webhook ante cambio de estado | El banner dejó de estar sin test: se asserta que no aparece con todo `ok`, que nombra el componente caído, que trata el subsistema `agents` como degradado y que nombra varios caídos a la vez sin inventar uno sano. Quedan tres huecos: el poll de 10 s (`refetchInterval: 10_000` existe en `SystemBanner.tsx` y ningún test lo verifica), la porción `agents` de `check_components` **del lado backend**, y `GET /health/components` como endpoint HTTP, que ningún test invoca. Dos criterios de UI no están implementados: timestamp del último check saludable y botón de cierre | `SystemBanner.test.tsx` (4), `test_notifications.py` (5), `test_health_n8n_check.py` (8); detalle [§5.28](../trazabilidad_us_tests.md#528-us-28-banner-de-degradación-del-sistema-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `SystemBanner.test.tsx` (4), `test_notifications.py` (5), `test_health_n8n_check.py` (8); detalle [§5.28](../trazabilidad_us_tests.md#528-us-28-banner-de-degradación-del-sistema-parcial) | PARCIAL |
| US-29 | Webhooks fallidos: Banner amarillo con `retry_count >= 3`; link a la vista; tabla con `event_id`, primer intento, error y `retry_count`; reintentar/descartar por fila; bulk; eliminación tras reintento exitoso; el banner desaparece; `audit_log` | Dos criterios cerrados. El reintento tiene por fin camino feliz, en dos capas: `test_retry_alert_resets_dlq_state_and_reschedules` verifica el reseteo de `failed_at`/`last_error`/`retry_count` y que la cascada se vuelve a disparar sobre la misma alerta y el mismo evento; `test_retry_alert_delivers_and_leaves_the_dlq` cierra el ciclo hasta que la fila desaparece de `list_failed_alerts`. Y el banner desaparece con la DLQ vacía. Persisten las divergencias: sin umbral `retry_count >= 3`, el link va a `/alerts` y no a `/notifications/failed`, no hay bulk ni `audit_log`, y la tabla `failed_notifications` no existe | `test_notifications.py` (9), `AlertsBanner.test.tsx` (5), `test_sse_alerts.py` (3); detalle [§5.29](../trazabilidad_us_tests.md#529-us-29-visualización-y-reintento-de-webhooks-fallidos-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_notifications.py` (9), `AlertsBanner.test.tsx` (5), `test_sse_alerts.py` (3); detalle [§5.29](../trazabilidad_us_tests.md#529-us-29-visualización-y-reintento-de-webhooks-fallidos-parcial) | PARCIAL |
| US-30 | Agente en shutdown graceful: Deja de aceptar eventos; drena con timeout 30 s; heartbeat con `shutdown: true`; backend marca `draining`; indicador "Drenando N eventos"; botones deshabilitados con tooltip; pasa a `offline`/`dead` | La transición `draining → offline` quedó cubierta con su caso negativo (un agente drenando que sigue latiendo no se marca offline): era la rama del `status.in_([online, draining])` que nunca se ejercitaba. Siguen sin test o sin implementación el cese de aceptación de eventos de `fanotify`, el timeout de 30 s del drenaje (`_drain_then_stop(..., timeout=30.0)` no lo invoca ningún test) y los dos criterios de interfaz | `test_shutdown_heartbeat.py` (5), `test_heartbeat_consumer.py` (3), `test_stability_fixes.py` (1), `test_agent_mgmt.py` (1), `test_publisher.py` (1), `test_reconnect_order.py` (2); detalle [§5.30](../trazabilidad_us_tests.md#530-us-30-indicador-de-agente-en-shutdown-graceful-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_shutdown_heartbeat.py` (5), `test_heartbeat_consumer.py` (3), `test_stability_fixes.py` (1), `test_agent_mgmt.py` (1), `test_publisher.py` (1), `test_reconnect_order.py` (2); detalle [§5.30](../trazabilidad_us_tests.md#530-us-30-indicador-de-agente-en-shutdown-graceful-parcial) | PARCIAL |
| US-31 | Toggle de superseded: Oculto por defecto; checkbox; ícono de cadena rota y `parent_event_id`; persistencia en la URL; `GET /events` respeta el parámetro | El vacío más significativo quedó cerrado: el criterio 5 (la regla W1 del lado servidor) lo assertan ahora dos tests con eventos `superseded` reales, uno por cada lado del flag. Persisten sin test los dos criterios de interfaz, y sigue vigente la advertencia de que `frontend/src/api/events.ts:88-97` **duplica** la serialización de filtros en un `paramsSerializer` sin ningún test | `test_event_listing_contract.py` (2), `eventFilters.test.ts` (7); detalle [§5.31](../trazabilidad_us_tests.md#531-us-31-toggle-para-mostrar-eventos-superseded-parcial) | Pendiente de procedimiento manual reproducible sobre versión congelada. | Algunos criterios están implementados/probados y otros siguen sin prueba o sin función. | `docs/trazabilidad_us_tests.md`; `test_event_listing_contract.py` (2), `eventFilters.test.ts` (7); detalle [§5.31](../trazabilidad_us_tests.md#531-us-31-toggle-para-mostrar-eventos-superseded-parcial) | PARCIAL |

## Resumen de línea base histórica/preliminar

| Completas | Parciales | Sin cobertura funcional | No evaluables |
|---:|---:|---:|---:|
| **4** | **27** | **0** | **0** |

La línea base histórica era 3/27/1/0. US-09 pasa de “sin cobertura funcional” a **completa** porque la implementación y su corrección fueron verificadas sobre un snapshot limpio. US-23 permanece parcial: las unidades n8n A/B acreditan el tramo controlado, persistencia, recuperación y fallback webhook, pero no SMTP real ni todos los campos de contexto del criterio.

### Evidencia experimental nueva que no altera el conteo de historias

- La corrida causal válida ejecutó 60 operaciones: 50 generaron eventos completamente correlacionados y persistidos; 10 retornaron a la baseline aprobada y fueron descartadas legítimamente como `matches_active_baseline`; no hubo ausencias nuevas inexplicadas. Esto verifica el contrato actual de detección, pero no reconstruye la causalidad de los 19 casos históricos.
- Run 3 conserva el resultado intermedio de 51,773 s (**NO CUMPLE**). Run 4 sobre backend Unidad 1 procesó 3.000/3.000, con 0 rechazos, 0 duplicados, 3.000 XADD/XREAD/HMAC/commit/XACK/event_ack y cola final 0, en 29,146335596 s (102,928891 eventos/s): **CUMPLE** `<30 s` sólo bajo las condiciones documentadas de laboratorio.
- La instrumentación causal quedó fijada en `aae55e4`; la cola/ACK en `7c5afa5` y backend Unidad 1 en `965dcac`. Los paquetes están identificados por manifiestos; 32,358 s fue una proyección, no una medición. Estos resultados no reclasifican historias.
- D49–D51 (`b70934d` + `b8e9513`) preservan atribución desconocida como `null`, hacen observable `FAN_Q_OVERFLOW` mediante `detection_gap` y mantienen `event_type`/path nulo a través de backend y frontend. `8d37075` liga la baseline aprobada al evento y sus bytes cifrados. Estas correcciones transversales no cambian por sí solas el conteo 4/27.

### Criterios de lectura

- **Completa:** todos los criterios sustantivos enumerados tienen una aserción pertinente.
- **Parcial:** queda al menos un criterio sin prueba o implementación.
- **Sin cobertura funcional:** no se acredita el flujo solicitado.
- **No evaluable:** no existe evidencia suficiente para clasificar.

> **Límite n8n:** US-23 no pasa a completa porque SMTP real y el payload de contexto completo no fueron acreditados. Los escenarios A/B deben conservarse como evaluaciones nuevas separadas de B4.
