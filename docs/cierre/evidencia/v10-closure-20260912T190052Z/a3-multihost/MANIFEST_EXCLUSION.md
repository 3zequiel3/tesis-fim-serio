# Manifest exclusion

This directory holds the working runbook, scripts and outputs of the manual two-host A-3 exercise, which is in progress.
Its files change while the exercise runs, so it is excluded from `../SHA256SUMS`. When the exercise finishes, this
directory receives its own `SHA256SUMS`, and the parent manifest is regenerated to include it.
