# Runtime dependencies — FIM Agent
# Pinned 2026-06-11. structlog, valkey, cryptography, pydantic-settings pinned to
# match backend versions.
#
# fanotify: se usa un backend interno sobre syscalls crudas (agent/_fanotify.py,
# ctypes → glibc), en modo FID (FAN_REPORT_DFID_NAME). No se depende de pyfanotify:
# su API pública (clase Fanotify + socket) es incompatible con el hilo lector
# bloqueante del detector, y el modo fd no entrega eventos de create/delete/move.
# Requiere Linux kernel >= 5.1, CAP_SYS_ADMIN y CAP_DAC_READ_SEARCH.

structlog==25.1.0
valkey==6.1.0
cryptography==44.0.2
pydantic-settings==2.7.1
pydantic==2.10.6
PyYAML==6.0.2
httpx==0.28.1
